# OpenClaw host contract used by PLUR1BUS 7.15.4 and its mapping onto the harness

**Status:** Phase 0 analysis · **Date:** 2026-09-22 · **Inputs:** `docs/phase0/brief.md` D2/D3/D4/D6/D7/D9, `docs/phase0/research/plur1bus-host-contract.md`, `docs/phase0/research/plur1bus-crons-embedding-portability.md`, `docs/phase0/research/openclaw-layout-dreaming-ui.md` §4.

**Sources of record**

| Repo | Path | Commit | Version |
|---|---|---|---|
| PLUR1BUS memory plugin | `/home/claude/refs/openclaw-plur1bus-memory` | `89148f9` (`89148f9f604a27149094efbc7cd910a7d362a94a`, 2026-09-21) | `@cyb3rb1ade/plur1bus-memory` 7.15.4, manifest id `memory-lancedb-namespaced` (`package.json:3`) |
| OpenClaw host | `/home/claude/refs/openclaw` | `b9421f4` (`b9421f4fa69f10404d21c5d55d07fdb0e86dff16`) | 2026.9.5 |

All `file:line` without a repo prefix are PLUR1BUS @ `89148f9`. Lines prefixed `oc:` are OpenClaw @ `b9421f4`. Declared host contract: `openclaw.compat.pluginApi ">=2026.8.1"`, `minGatewayVersion "2026.8.1"`, `build.pluginSdkVersion "2026.8.2"` (`package.json:8-20`, via research §0). Every row below was read out of the cited file; rows that could not be confirmed are marked **unverified**.

Naming convention for the harness column: `Engine.*` = the host-neutral PLUR1BUS engine API proposed in `docs/engine-extraction.md` §b; `Host.*` = the harness-side interface the engine calls back into; `Adapter` = code that stays in `@cyb3rb1ade/plur1bus-host-openclaw`.

---

## (a) Every host surface

### a.1 Hooks (registration counts from `grep -c "api\.on("`; 30 `api.on` sites total, research §9)

| Surface | file:line @ 89148f9 | Semantics | Timing/budget | Blocking? | Classification | Harness mapping |
|---|---|---|---|---|---|---|
| `before_prompt_build` — auto-recall + injection (**1 of 3**) | `index.js:12285-13351` | `(event, ctx)`; reads `event.prompt/messages/runner/provider/sessionKey/sessionId/runId/compactedAt`, `ctx.agentId/workspaceDir/workspaceKey/sessionKey/sessionId/runId/messageProvider/chatId`. Returns `{ prependContext }` or `undefined`; never mutates `event` | `{ timeoutMs: recallTimeoutMs + 5_000 }` = **50 000 ms** (`index.js:13351`, `lib/runtime-scheduler.js:7`); internal soft budget **35 000 ms** (`index.js:4711`); phase timer `index.js:12292-12296` | Yes — awaited before prompt assembly; on timeout returns cached recall or `undefined` | Adapter (registration + return shape); engine (pipeline) | `Engine.recall(query, principal, budget) → ContextBlock[]`, called by the harness turn assembler; deadline + `AbortSignal` passed in, not derived |
| `before_prompt_build` — reply-outcome completion (**2 of 3**) | `index.js:12216-12251` | Closes the previous turn's pending reply outcome, using this turn's `event.prompt` as the user's reply. Registered only when `replyOutcomeEnabled`; DB work deferred to `replyOutcomeDynamics.enqueue` (`index.js:12223-12226`) | **No `timeoutMs`** → host default **15 000 ms** (oc:`src/plugins/hooks.ts:148`) | Yes — runs sequentially *before* recall (`index.js:12223`) | Adapter wrapper / engine body | Folded into `Engine.capture` close-out; the harness delivers the previous turn's outcome as an explicit event, not by reading the next prompt |
| `before_prompt_build` — maintenance-only fallback (**3 of 3**) | `index.js:13354-13443` | `else` branch when `autoRecall` is off but `neoEnabled \|\| schicht15Enabled \|\| gcEnabled`; still returns `{ prependContext: [startNotice, nudge+conflictNudge, timeContext, temporalContinuityContext, reminderNudge] }` (`index.js:13443`) | none declared → 15 000 ms host default | Yes | Adapter | Same engine entry point with `recall: false`; no second registration |
| `agent_end` — auto-capture (**1 of 3**) | `index.js:10354-11299` | Incognito classification first, fail-closed (`index.js:10363-10375`); builds `memoryCtx` from 13 `ctx` fields (`index.js:10396-10409`); returns the capture promise | `{ timeoutMs: 60_000 }` (`index.js:11299`), work inside `runtimeScheduler.enqueueCapture` with an `AbortSignal` | Yes, up to 60 s | Adapter boundary, engine body | `Engine.capture(turn, principal)`; the harness passes a typed `TurnRecord` and a canonical principal, so no `ctx`-field archaeology |
| `agent_end` — reply-outcome recording (**2 of 3**) | `index.js:11304-11321` | `lastMessageText(event.messages, ["assistant"])` appended to the pending outcome; skips background turns and turns without `ctx.workspaceDir` | none → host default **30 000 ms** (oc:`src/plugins/hooks.ts:118`) | Yes (sync) | Adapter wrapper | `Engine.capture` sub-step |
| `agent_end` — turn-route cleanup (**3 of 3**) | `index.js:12278-12283` | `turnRoutes.clearRun(ctx.runId ?? event.runId)` | none | Yes (trivial) | Adapter | **Deleted** — the harness owns the transport and supplies the principal directly |
| `reply_dispatch` | `index.js:12259-12276` | Mints the turn-route ticket later consumed by `resolveHostHookMemoryContext`; reads `event.sessionKey/runId` and 17 `event.ctx.*` fields plus `hookCtx.dispatchKind`; always returns `undefined` | `{ priority: Number.MIN_SAFE_INTEGER, eligibleDispatchKinds: ["agent","acp"] }` (`index.js:12275`); no `timeoutMs` | Observational, but must run **before** the matching `before_prompt_build` or identity degrades to `safeHookBase` | Adapter — the single hardest port | **Deleted.** Replaced by ADR-007's canonical principal handed to `Engine.recall`/`Engine.capture` |
| `before_dispatch` + `before_agent_reply` — critical-push quoted reply | handler `index.js:10068-10104`, registered in a loop `index.js:10105-10111` | **Claiming hook**: returns `{ handled: true, text, reply: { text } }` to short-circuit the turn, else `undefined`. Runs `runCriticalCommand` inline (DB read) | none declared; claiming hooks get **no** host default (oc:`src/plugins/hooks.ts:630-631`) → unbounded | Yes, pre-empts the whole turn | Adapter | `Host.registerTurnInterceptor(fn)` in the harness; one registration, one name, explicit deadline |
| `before_agent_reply` — unsafe direct feature-cron guard | `index.js:4534-4543`, predicate `index.js:3435-3444` | Registered **only** when `!cronDirectDispatchReady`; claims `context.trigger === "cron"` turns matching a known feature-cron body, returning `{ handled:true, reply:{ text:"NO_REPLY" } }` | none | Yes, claiming | Adapter | **Deleted** — the harness scheduler (D4) never routes a job through the model |
| `gateway_start` — feature-cron bootstrap | `index.js:7047-7070`, guard `index.js:7043-7046` | Epistemic cutoff, unsafe-cron reconcile via `gatewayContext.getCron()`, then deferred CLI bootstrap on an unref'd `setTimeout` at **90 000 ms** (native ready) or **0 ms** | `{ timeoutMs: cronDirectDispatchReady ? 5_000 : 30_000 }` (`index.js:7069`) | Awaited within that budget; real work deferred | Adapter | **Deleted** — `Engine.jobs` is provisioned in-process by the harness scheduler |
| `gateway_start` — Neo worker warm-up | `index.js:5300-5306`, constant `index.js:5296` | `neoWorkerRuntime.warmUp()` after `NEO_WORKER_WARMUP_DELAY_MS = 20_000` | `{ timeoutMs: 5_000 }` | Non-blocking (unref'd timer) | Adapter | `Engine.open(agentId)` warms the worker; supports D6 (warm daemon) |
| `gateway_start`/`gateway_stop` — control-plane health | `index.js:9585-9586` | `controlHealth.start()` / `.stop()` | `{ timeoutMs: 5_000 }` both | Yes | Adapter | `Engine.open`/`Engine.close` |
| `gateway_start`/`gateway_stop` — Neo service | `index.js:10332-10333`, fallback `index.js:10334-10340` | `startNeoService`/`stopNeoService`; falls back to `api.registerService({id:"plur1bus-neo-maintenance",…})` | `{ timeoutMs: 30_000 }` both | Yes | Adapter | `Engine.open`/`Engine.close` |
| `gateway_start`/`gateway_stop` — Obsidian bridge fallback | `index.js:7027-7028`; preferred path `index.js:7024-7025` | Used only when `api.registerService` is absent | `{ timeoutMs: 30_000 }` both | Yes | Adapter | `Engine.open`/`Engine.close` |
| `gateway_stop` — runtime resource shutdown | `lib/runtime-shutdown.js:308`, disposals `:243-297` | Idempotent `shutdownOnce`: DB adapter, pools, shared pool, turn routes, metrics flush, LLM result cache, scoped-embedding IPC, embedding provider, reranker, local-model generation, model-preparation coordinator, re-embedding coordinator | `{ timeoutMs: 30_000 }`; host default for `gateway_stop` is **5 000 ms** (oc:`src/plugins/hooks.ts:136`), overridden by the plugin value (oc:`src/plugins/hooks.ts:616-621`) | Yes — LanceDB writes can be lost if not awaited | Adapter (ordering) + engine (disposals) | `Engine.close()` returns a promise; the harness gives it a real, configurable teardown budget |
| `skill_proposal_changed` | `index.js:5939-5965` | `(event, context)`; reads `event.proposal.id`, `event.action`; rethrows on failure | `{ registrationId:"plur1bus-skill-workshop-lifecycle-v1", timeoutMs: 30_000 }` (`index.js:5961-5963`) | Yes | Adapter | `Host.onSkillProposalChanged` optional capability; no-op when the harness has no skill workshop |

**What breaks without each hook** (research §1, verbatim summary): no `before_prompt_build` → no injection at all; no `agent_end` → no automatic capture (only `memory_store` and the `scripts/auto-capture-lancedb.mjs` cron fallback, documented `index.js:15-18`); no `reply_dispatch` → every hook-originated request degrades to `safeHookBase`, i.e. agent-private scope only (`lib/memory-request-context.js:1405-1417`); no `gateway_*` and no lifecycle/service registrar → `registerGatewayShutdown` returns `false` (`lib/runtime-shutdown.js:238`) and the Neo worker, local models and the embedding socket leak across reloads.

### a.2 `prependContext` injection contract and the six named blocks

All dynamic material reaches the model through **one** return value. The system prompt is not rewritten per turn; the only stable system-prompt contribution is `registerMemoryPromptSupplement` (constants).

| Surface | file:line @ 89148f9 | Semantics | Timing/budget | Blocking? | Classification | Harness mapping |
|---|---|---|---|---|---|---|
| Assembly point | `index.js:13315-13325` | `return { prependContext: applyGlobalInjectBudget({ blocks: [...], maxChars: cfg.recall?.globalInjectMaxChars ?? 17_000 }) }` | **17 000 chars** | — | Engine return, adapter shape | `Engine.recall(...) → { blocks: ContextBlock[], capChars }`; the harness joins and applies the cap so it can also budget tokens |
| Block 1 `neo` | `index.js:13317`; built `index.js:13301-13313` | `formatNeoRecallContext(deduped.lanes, {idempotencyKey})`, deduped against injected LanceDB memories | embedding sub-budget, sentinel `NEO_EMBED_TIMEOUT` (`index.js:5298`) | yes | Engine | `droppable: true` |
| Block 2 `start` | `index.js:13318`; built `index.js:12425-12427`, `:13380-13382` | One-shot `<plur1bus-start-notice>` consumed from a file under `OPENCLAW_HOME \|\| ~/.openclaw` | — | yes | **Adapter** (env var) | Harness state dir injected as `Host.stateDir` |
| Block 3 `memories` | `index.js:13319`; composite at `index.js:13214` | `[personaDirective, moodStyleDirective, reactionDirective, dreamEchoContext, openThreadsContext, contradictionDisclosureContext, memoriesContext, reactivationContext].join("\n\n")` + `nudge + conflictNudge + skillProposalNudge` | inside the recall budget | yes | Engine | `droppable: true`; sub-blocks become named `ContextBlock`s so the harness can drop finer-grained |
| Block 4 `time` | `index.js:13320`; built `index.js:13261` | `formatTimeContext(agentId, wsKey, ctx.workspaceDir, lang)`; `recordActivity` runs *after* formatting (`index.js:13270`) | — | yes | Engine (`lib/session-time.js`) | `droppable: false` |
| Block 5 `temporal` | `index.js:13321`; built `index.js:13262-13269` | `formatTemporalContinuityContext(...)`, gated on `cfg.temporalContext` | — | yes | Engine (`lib/temporal-context.js`) | `droppable: false` |
| Block 6 `reminder` | `index.js:13322`; built `index.js:13272-13296` | Merges DB-due reminders with a pending file, dedupes by id, marks presented | — | yes | Engine | `droppable: false` |
| Budget algorithm | `lib/inject-budget.js:9-35` (35 lines) | Drops/truncates the **last** droppable block first until the join fits; never touches non-droppable blocks | — | — | Engine | Port verbatim; add a token-aware mode for D7 |
| Recall block `<relevant-memories>` | `lib/relevant-memory-context.js:229`, called `index.js:13054`; items `:209-211`; preamble `:27-38` | `<relevant-memories untrusted="true" mode="historical-evidence-only">` wrapping `<memory-record><quoted-evidence>`; optional `<memory-semantic-lens>` (`:236`) | inside recall budget | yes | Engine | unchanged |
| Reactivation after pause | `index.js:12934-12975` | `runConversationReactivationRecall({..., compactedAt: event?.compactedAt \|\| ctx?.compactedAt \|\| null, ...})` | **hard `Promise.race` at `crrCfg.timeoutMs ?? 50` ms** (`index.js:12967-12969`), rejects `crr_timeout` | yes | Engine (`lib/conversation-reactivation-recall.js`, 961 lines) | unchanged; the 50 ms race becomes an engine-internal budget parameter |
| Compaction signal | `index.js:12959` | The **only** compaction input: `event.compactedAt \|\| ctx.compactedAt` | — | — | Adapter input field | `Engine.checkpoint(agentId, reason)` — an explicit call, see §e |
| Fallback injection on recall failure | `index.js:13329-13330` | On a thrown recall, still returns `[neoContext, startNoticeContext]` if non-empty | — | — | Adapter | Engine returns partial blocks with a `degraded` flag |
| Static system-prompt supplement | `index.js:7073-7088` | `api.registerMemoryPromptSupplement(() => [...])`; without Neo one line (`:7077`), with Neo 4 fixed lines (`:7082-7088`). Returns constants — stable, cache-friendly | — | no | Adapter registration, engine strings | `Engine.systemSupplement() → string[]`, placed in the cached prefix (D7) |
| Corpus supplement | `index.js:7090-7110`, record shape `:7150-7156` | `api.registerMemoryCorpusSupplement({ async search(params) })`, Neo-only; returns `{id, provenanceLabel, sourceType, updatedAt}` | — | host-driven | Adapter | Optional `Host.corpusSupplement` capability |

**Cache-friendliness (D7).** OpenClaw's own result type already offers `prependSystemContext` / `appendSystemContext` documented as "Prepended to the agent system prompt so providers can cache it (e.g. prompt caching). Use for static plugin guidance instead of prependContext to avoid per-turn token cost" (oc:`src/plugins/hook-before-agent-start.types.ts:41-50`). PLUR1BUS uses `registerMemoryPromptSupplement` for that role instead. The harness should expose both lanes explicitly: `Engine.systemSupplement()` (cached prefix) and `Engine.recall()` (volatile suffix).

### a.3 Claiming hooks, tools, LLM seam

| Surface | file:line @ 89148f9 | Semantics | Timing/budget | Blocking? | Classification | Harness mapping |
|---|---|---|---|---|---|---|
| Claiming: critical-push quoted reply | `index.js:10068-10111` | see a.1 | none (oc:`src/plugins/hooks.ts:630-631`: claiming hooks have no default timeout) | pre-empts turn | Adapter | `Host.registerTurnInterceptor` with a mandatory deadline |
| Claiming: cron guard | `index.js:4534-4543` | see a.1 | none | pre-empts turn | Adapter | deleted |
| `api.registerTool(factory, { names })` | `index.js:11328`, options `index.js:12167-12169` | Factory per tool-ctx; array filtered by `guardWorkspaceTools(workspaceTools, workspacePolicyGuard.decision(memoryCtx))` (`index.js:12166`); names `memory_recall, memory_search, memory_store, memory_forget, knowledge_update` | — | — | Adapter | `Engine.tools → { schema, handler }[]`, host-agnostic; the harness adapts to MCP/ACP tool shapes |
| `memory_recall` | `index.js:11339-11352`; `validAt` `:11396-11403` | `{query (req), limit, full_text, validAt}`; `validAt` caller-supplied only, never defaulted to "now"; `limit` via `normalizeBoundedRecallInteger(…, 1, 100)`. Also the declared `deterministicRecallToolName` | recall budget | yes | Engine | unchanged |
| `memory_search` | `index.js:11492-11502` | — | — | yes | Engine | unchanged |
| `memory_store` | `index.js:11503+`; origin enum `index.js:11512` | Origin enum description leaks channel semantics: `'dm'`, `'group' = Telegram group chat`, `'cron'`, `'internal'` | — | yes | Engine + channel vocabulary | Origin becomes `TurnOrigin` from the canonical principal, not a free-text enum |
| `memory_forget` | `index.js:11765+`; gate `index.js:11332-11337` | Blocked unless `security.allowModelDestructiveMemoryOps`, because "model-facing tool calls do not carry a user-bound authorization context" | — | yes | Engine | Unblocked in the harness **only** when the principal is user-bound (ADR-007) |
| `knowledge_update` | `index.js:11908-11911`; consolidation `:12095-12117`; timeout `:12072`, `:12107` | Curates pending memories into `memory/KNOWLEDGE.md` under a lock; past 200 lines consolidates to ≤150; `resolveKnowledgeUpdateTimeoutMs(maxTokens)` | per-call LLM timeout | yes | Engine + LLM | unchanged |
| Tool return shape | — | Host's: `{ content: [{ type:"text", text }] }` | — | — | Adapter | Engine returns structured results; the adapter wraps |
| `runtimeLlm` handed to router | `index.js:4602` | `runtimeLlm: runtimeIfUsable(api)?.llm` passed once into the router | — | — | **Adapter** | `Host.llm.complete(params)` — one injected interface |
| Capability probe | `index.js:4882`, `index.js:4938` | `typeof runtimeIfUsable(api)?.llm?.complete === "function"` gates every LLM-dependent feature | — | — | Adapter | `Host.llm` presence |
| **The entire host-LLM seam** | `lib/llm-router.js:412-450` (~40 lines of 464) | `const runtimeLlm = …callOptions?.runtimeLlm ?? route.runtimeLlm; … await bounded.waitFor(runtimeLlm.complete(params))` | `bounded.waitFor` + `route.timeoutMs` | yes | Adapter | Replace `runtimeLlm` with `Host.llm`; the router itself is engine |
| Model selection | `index.js:4575-4585`; route kinds `lib/llm-router.js:311-346` | Features without own transport/model inherit `cfg.llmRouter.defaultModel`; a feature with own `baseUrl`/`apiKey`/`headers` is never given a foreign model | — | — | Adapter | Same policy, `Host.defaultModel` |
| Direct-provider fallback | `lib/llm-call.js:27-80`; timeout `:3` | OpenAI-compatible client, `DEFAULT_LLM_TIMEOUT_MS = 30_000`, `jsonMode`, `disableThinking`, `signal`, reads `reasoning_content` when `content` is empty | 30 s | yes | Engine | kept as the engine's own provider path (D9 independence) |
| LLM result cache | `index.js:4556-4565` | `createLlmResultCache({…, maxBytes: 67_108_864})` | 64 MiB | — | Engine | D7 layer 3 |
| No host LLM | `lib/runtime-shutdown.js:35-49`; dormant report `index.js:5315-5319` | `runtimeIfUsable` probes `runtime.config` rather than `api.registrationMode`; features with no route are reported dormant | — | — | Adapter | Harness always supplies `Host.llm` |

### a.4 Memory capability, `classifyWorkspaceMemoryPaths`, MEMORY.md / USER.md

| Surface | file:line @ 89148f9 | Semantics | Timing/budget | Blocking? | Classification | Harness mapping |
|---|---|---|---|---|---|---|
| `api.registerMemoryCapability({...})` | `index.js:4514-4518`, guard `:4446`, else-branch `:4519-4523` | `{ deterministicRecallToolName:"memory_recall", supportsPrivateTranscriptRecall:false, runtime: memoryHostRuntime }` | — | — | Adapter | **Deleted** — in the harness PLUR1BUS *is* the memory owner, not a registered slot |
| Runtime construction | `index.js:4451-4513`; closures `lib/setup/memory-host-runtime.js:113-118` | `createMemoryHostRuntime({logger, hostConfig, dbPath, provider, embed, cardCount, readCard, recall})` — every dependency a closure so the runtime registers before pools exist | — | — | Adapter | Direct engine construction, no closure indirection |
| `getMemorySearchManager({agentId, purpose})` | `lib/setup/memory-host-runtime.js:238-256`; id regex `:30` | `{ manager, debug:{backend:"builtin",purpose,managerMs} }` or `{ manager:null, error }`; `agentId` must match `/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/` | — | host-driven | Adapter | `Engine.open(agentId) → AgentStore` |
| `manager.search(query, opts)` | `lib/setup/memory-host-runtime.js:163-182`; bounds `:28-29` | `opts.{sources,maxResults,minScore,signal}`; `maxResults` 1..50, default 8. **Host `signal` accepted and not propagated** (`:170-171`) | — | yes | Adapter | Defect — see §f.1 |
| `manager.readFile({relPath, from, lines})` | `lib/setup/memory-host-runtime.js:183-201`; scheme `:23` | Resolves only `plur1bus://<agentId>/<cardId>`; nothing from disk | — | yes | Adapter | `Engine.readCard(id)` |
| `manager.status()` | `lib/setup/memory-host-runtime.js:202-219`, fallback text `:4-7` | `{backend:"builtin", provider, model?, workspaceDir?, dbPath?, dirty:false, sources:["memory"], …}` | — | — | Adapter | `Engine.status()` |
| Embedding probe | `lib/setup/memory-host-runtime.js:137-154`; constants `:24-26` | `PROBE_TEXT "PLUR1BUS embedding readiness probe"`, `PROBE_TIMEOUT_MS 10_000`, `PROBE_CACHE_MS 300_000` | **10 s / 5 min cache** | — | Adapter | `Engine.embedding.probe()` with the same budget |
| **Deliberately not frozen** | `lib/setup/memory-host-runtime.js:156-159`, `:236` | "The host wraps the manager in a Proxy whose `get` returns a bound copy of every function; a frozen … property makes that violate the Proxy invariant" | — | — | Adapter constraint | The harness must **not** Proxy-wrap engine objects; state it in the harness contract |
| `classifyWorkspaceMemoryPaths(params)` | `lib/setup/memory-host-runtime.js:264-269` → `lib/setup/workspace-memory-provenance.js:68-82` | One `{relativePath, originClass}` per input, in input order, never throws per path | — | host-driven | **Adapter — the whole MEMORY.md/USER.md seam** | Harness owns file-memory injection itself; the classifier's *rules* move into `Engine.classifyWorkspacePaths()` as a reusable policy |
| Classification rules | `lib/setup/workspace-memory-provenance.js:35-60`; eligible classes `:22` | `realpath` both sides; outside workspace → `untrusted`; `DREAMS.md`/`dreams.md`/`memory/dreaming/**`/`memory/.dreams/**` → `system`; `MEMORY.md`/`memory.md`/`USER.md`/`memory/**.md` → `agent`; else `untrusted`. `ELIGIBLE_MEMORY_ORIGIN_CLASSES = ["owner","agent"]` | — | — | Engine rules, adapter shape | ported as policy |
| Why it exists | `lib/setup/workspace-memory-provenance.js:1-17` | Without the method the host answers `unsupported`, logs "excluding automatic memory context: selected memory runtime does not support provenance classification" and drops both files. **PLUR1BUS therefore never reads or writes MEMORY.md/USER.md itself — it authorizes the host to inject them** | — | — | Adapter | see §e.4 |
| `authorizeSearchHits({hits})` | `lib/setup/memory-host-runtime.js:270-272` | Drops every hit with `source === "sessions"` — the enforcement behind `supportsPrivateTranscriptRecall:false` | — | — | Adapter | Harness ACL applies the same rule at the store boundary |
| `resolveMemoryBackendConfig()` | `lib/setup/memory-host-runtime.js:257-259` | `{ backend:"builtin" }` | — | — | Adapter | deleted |
| Workspace dir resolution | `lib/setup/memory-host-runtime.js:104-111` | Reads `hostConfig().agents.entries[agentId].workspace`, else `agents.defaults.workspace` — **direct dependency on OpenClaw's config shape** | — | — | Adapter | `Host.workspaceDir(agentId)` |
| KNOWLEDGE.md (PLUR1BUS-owned) | `index.js:3921` (`KNOWLEDGE_MD_FILE = "memory/KNOWLEDGE.md"`), `:4097`, `:12029`, atomic write `:12125-12127` | Written by PLUR1BUS under a lock with tmp+rename and YAML frontmatter; read back as the canonical-first recall lane | — | — | Engine | unchanged |

### a.5 Gateway/RPC methods, CLI registration, cron provisioning, control UI, shutdown, config, plugin-SDK deep imports

| Surface | file:line @ 89148f9 | Semantics | Timing/budget | Blocking? | Classification | Harness mapping |
|---|---|---|---|---|---|---|
| `plur1bus.control.status` | `lib/setup/control-ui-plugin-runtime.js:10`, registered `:1374-1392`, validator `:1360-1365` | `{scope:"operator.read"}`; params must be exactly `{}`; error `plur1bus_control_status_unavailable` | — | — | Adapter | Harness API `GET /api/plur1bus/status` (ADR-004) |
| `plur1bus.workspacePolicy.{get,list,set}` | `lib/setup/workspace-policy-plugin-runtime.js:185-187`, hard requirement `:178-183` | get/list read, set write; throws at registration if `registerGatewayMethod`/`registerCli` are missing | — | — | Adapter | `Engine.admin.workspacePolicy.*` |
| `plur1bus.obsidian.{detect,prepare,confirm}` | `lib/setup/obsidian-vault-plugin-runtime.js:192-194` | detect read; prepare/confirm write | — | — | Adapter | `Engine.admin.obsidian.*` |
| `plur1bus.reembedding.{plan,apply,resume,rollback,status,switch}` | `lib/setup/reembedding-plugin-runtime.js:217-252` | Loop over a handler map; skipped entirely if the registrars are absent | — | — | Adapter | `Engine.admin.reembedding.*` |
| `plur1bus.feature.run`, `plur1bus.command.run` | `lib/setup/feature-cron-plugin-runtime.js:339-354` | `{scope:"operator.write"}`; `resolveWorkspaceDir` via `runtime.agent.resolveAgentWorkspaceDir` | `FEATURE_CRON_TIMEOUT_MS = 540_000` (`lib/setup/feature-cron-plugin-runtime.js:20`) | — | Adapter | `Engine.jobs.run(job, agentId)` and `Engine.commands.run(name, args, principal)` |
| CLI registrations (5 commands) | `lib/setup/feature-cron-plugin-runtime.js:355` (`plur1bus-command`), `:386` (`plur1bus-feature-cron`); `lib/setup/workspace-policy-plugin-runtime.js:225`; `lib/setup/obsidian-vault-plugin-runtime.js:196`; `lib/setup/reembedding-plugin-runtime.js:254` | Commander shape `api.registerCli(({program}) => program.command(...))`. `plur1bus-command` requires a direct chat session key `agent:<id>:<channel>:<account>:direct:<peer>` | — | — | Adapter | Harness CLI is first-party (D6 <100 ms cold start); the engine exposes `Engine.commands` and the CLI binds them lazily |
| Manifest-declared CLI commands | `openclaw.plugin.json` → `cliCommands` | `plur1bus-command`, `plur1bus-feature-cron`, `plur1bus-obsidian`, `plur1bus-reembedding`, `plur1bus-workspace` | — | — | Adapter | — |
| Chat command registrar | `index.js:7172-7178` | `registerPluginCommand(spec)` wraps `api.registerCommand(spec)` **and** records the spec in `pluginCommandHandlers` so the same handler serves the operator CLI | — | host short-circuits the turn | Adapter | `Engine.commands` registry consumed by both surfaces |
| Top-level user commands | `/state` `index.js:9224-9231` (note: *"'/status' is reserved by OpenClaw"*, `:9226`); `/enable` `:9232`; `/disable` `:9239`; `/speaker` `:9247`; `/memory` `:10231`; `/mf` `:10238`; `/forget` `:10245`; `/correct` `:10252`; `/share`+`/teile` `:10260`; `/wiki` `:10268` | `channels: ["telegram","discord","slack","mattermost"]` throughout | — | yes | Adapter registration, engine handlers | Harness owns the command namespace → `/status` collision disappears; keep `/state` as an alias |
| `/plur1bus …` family | `index.js:9090-9122`, runners `:9114-9119` | 15 specs with `prefixTokens`; `channels` additionally `"cron"` (`index.js:9112`) | arg-length guard | yes | Adapter + engine | same registry |
| Action classification (deny-by-default) | `index.js:7216-7220`, `:7221-7231`, `:7232-7248`, `:7249-7252`; policy comment `:7213-7215` | "a new action must be added to one of these predicates before it may acquire a store or other memory-bearing dependency" | — | — | **Engine** (pure tables) | port verbatim |
| Cron-context detection | `index.js:7195-7201` | `channel === "cron"`, or `origin/source/kind === "cron"`, or `sessionKey` matching `/^agent:[^:]+:cron(?::\|$)/` | — | — | Adapter (session-key grammar) | replaced by typed `TurnOrigin` (§c) |
| Reply shaping for cron | `index.js:9079-9081` | `runOperatorCommand` coerces an empty result to `"NO_REPLY"` | — | — | Adapter | harness jobs return structured results, not chat text |
| **Cron provisioning — 11 specs** | `lib/setup/feature-cron-plan.js:25-170` (`REQUIRED_FEATURE_CRONS`; `feature:` keys at `:28,42,54,64,83,97,110,121,131,147,157`) | `persona-evolve, afterthought, consolidate-daily, auto-accept-stale, embedding-drain, emotion-refine, classify-recent, rem-dream, skill-miner, discover-semantic-links, gc-run`; `gc-run` is the only `singleton:true` (`:163-168`, research crons §1) | per-spec cron/every + tz | — | Adapter plan, engine jobs | `Engine.jobs` registry owned by the harness scheduler (D4) |
| Provisioning path | `scripts/setup-feature-crons.mjs` → `openclaw cron add` → `scripts/run-feature-cron.mjs` → Gateway `plur1bus.feature.run` → `/plur1bus internal <feature>` (`index.js:7498-8272`) | `run-feature-cron.mjs:1-20` imports `executeFeatureCronCli, loadOpenClawGatewayRuntime, parseFeatureCronRunnerArgs` from `lib/setup/feature-cron-plugin-runtime.js` | `--timeout-seconds 600`, `--output-max-bytes 65536` (`scripts/setup-feature-crons.mjs:140-147`); `--session isolated` (`:149`); `--no-deliver` pinned when delivery is absent (`:160-164`) | — | Adapter + out-of-process script | **Deleted.** `Engine.jobs.run(job, agentId)` called in-process by the harness scheduler; no CLI, no RPC, no child process |
| Setup-script contract | `scripts/setup-feature-crons.mjs:13-17` | "this script must NEVER fail an install… exits 0"; partial failures are warnings and also exit 0 | — | — | Adapter | Harness provisioning must fail loudly and persist a result record (D4) |
| **Native-capability probe** | `scripts/setup-feature-crons.mjs:48` (`NATIVE_PROBE_TIMEOUT_MS = 30_000`), probe `:57-72`, rationale `:41-47` | Requires `cron add --help` to contain `--command-argv`, `--timeout-seconds`, `--output-max-bytes`; `plur1bus-feature-cron --help` to contain `plur1bus-feature-cron`, `--agent`, `--feature`; and `existsSync(FEATURE_CRON_RUNNER_PATH)`. Throws otherwise → the caller **safety-disables** existing jobs. Budget was raised from 5 s to 30 s because the host CLI boot took 11.6 s | 30 s | — | Adapter | **Deleted.** No `--help` string parsing anywhere in the harness |
| `api.registerHttpRoute` | `lib/setup/control-ui-plugin-runtime.js:1403-1408`, path constant `:13` | `{path:"/plugins/memory-lancedb-namespaced/control", auth:"gateway", match:"exact", handler}` | — | — | UI adapter | Harness route `/plur1bus` under the harness API auth (ADR-004) |
| **Control-UI descriptor** | `lib/setup/control-ui-plugin-runtime.js:1394-1424`; dual-path comment `:1390-1393` | Resolved from `api.registerControlUiDescriptor` **or** `api.session.controls.registerControlUiDescriptor` — reading only the nested path silently registered no tab on 2026.8.2. Descriptor `{surface:"tab", id:"plur1bus", label:"PLUR1BUS", description, path, icon:"database", group:"control", requiredScopes}`; scopes `["operator.read"]` read-only else `+["operator.write"]` (`:1417-1423`). Missing capability → `{tabRegistered:false}` + warn (`:1397-1400`) | — | — | UI adapter | Harness UI mounts the renderer as a first-class route; descriptor concept disappears |
| Control-UI renderer + copied tokens | `lib/setup/control-ui-plugin-runtime.js` (1426 lines); HTML/CSS ≈ `:985-1120`; token block `:991-1013`; comment `:987-990` | "The tab is an iframe with an opaque origin: it cannot read the host's stylesheet and receives no theme message. The Control UI tokens are copied here under their own names, dark first like the host, light on the OS setting." `docs/compatibility-openclaw.md:306` records the values as "value-identical" across 2026.8.2/2026.9.1 — i.e. a hand-maintained copy that drifts silently | optional `<meta http-equiv="refresh">` (`:984`) | — | **UI** | `@cyb3rb1ade/plur1bus-control-ui` with a token module; harness supplies real tokens, no iframe copy |
| Services | `api.registerService` ×11: `index.js:7024-7025`, `:10334-10340`; `lib/providers/scoped-embedding-ipc.js:708-714`; `lib/runtime-shutdown.js:332-345`, `:383-400`, `:425-440` | `{id, start, stop}` | host-driven | — | Adapter | `Engine.open`/`Engine.close` + `Engine.services` list |
| Runtime lifecycle | `lib/runtime-shutdown.js:232-236`, `:300-306` | Preference order `api.lifecycle.registerRuntimeLifecycle` → `api.registerRuntimeLifecycle` → `api.on("gateway_stop")`; id `plur1bus-runtime-resources` | `{timeoutMs: 30_000}` on the hook path | yes | Adapter | single `Engine.close()` |
| Config schema | `openclaw.plugin.json` → `configSchema.properties` | 56 top-level keys (enumerated in research §7) | — | — | **Engine config, adapter-delivered** | Same schema, delivered as a typed object by the harness; JSON Schema kept for the OpenClaw adapter |
| `configContracts.secretInputs` | `openclaw.plugin.json` → `configContracts` | 8 secret paths: `embedding.apiKey`, `embedding.fallback.apiKey`, `reranker.apiKey`, `merging.apiKey`, `schicht15.apiKey`, `skillMiner.apiKey`, `criticalPush.apiKey`, `emotion.t3.apiKey` | — | — | Adapter | ADR-005 secret store |
| `uiHints` | `openclaw.plugin.json` → `uiHints` | 17 keys: the 8 secrets (`sensitive+advanced`) plus 9 operational toggles | — | — | UI adapter | harness settings schema |
| Config mutation | `index.js:6158` | `runtimeIfUsable(api)?.config?.mutateConfigFile` gates the whole re-embedding switch flow | — | — | Adapter | `Host.config.mutate(patch)` |
| Session entry **read** | `index.js:12322`, `:9297`, `:9314`, `:9333`; workspace fields `:9327-9331` | `runtime.agent.session.getSessionEntry({agentId, sessionKey, readConsistency})`; workspace from `spawnedCwd \|\| spawnedWorkspaceDir \|\| worktree.canonicalWorkspaceDir` | measured; warn ≥ 1000 ms (`lib/memory-request-context.js:1332-1342`) | yes | Adapter | Deleted — the harness passes the principal and workspace directly |
| Embedding provider registration | `lib/providers/openclaw-memory-embedding-adapters.js:317-324`; ids `:15-19` | `if (typeof api.registerEmbeddingProvider !== "function") { warn; return; }` — **optional**; ids `plur1bus-openai`, `plur1bus-openai-compatible`, `plur1bus-e5-small` | inline **60 s query / 120 s batch** (`:23-24`), `MAX_EMBED_CHARS = 10_000` (`:25`) | — | Adapter (optional) | `Engine.embed()` is the single owner (D9); the adapter re-exports it to OpenClaw |
| **Plugin-SDK deep imports** | loader `lib/setup/feature-cron-plugin-runtime.js:217-234`, allowlist `:221` | `loadOpenClawPluginSdkRuntime(subpath)` resolves the **active OpenClaw package manifest**, asserts `manifest.name === "openclaw"`, then `createRequire(manifestPath).resolve("openclaw/plugin-sdk/<subpath>")`. Allowlist: `gateway-runtime`, `secret-input-runtime`, `memory-host-events` (`:219-221` notes the last is "the host's public event log, used by the dream diary bridge") | — | — | **Adapter — a hard host-*package* dependency, not just an API** | All three become harness-native: job dispatch, secret store (ADR-005), event log |
| Routing SDK import | `lib/memory-request-context.js:346`, `:368`, `:339-342`; expected exports `:18-23` | `import("openclaw/plugin-sdk/routing")` for `parseAgentSessionKey`, `parseThreadSessionSuffix`, `normalizeOptionalAccountId`, `normalizeMessageChannel`, `isIncognitoSessionKey` | — | — | **Adapter** | Deleted with the ticket subsystem (§c) |
| Environment variables | `OPENCLAW_HOME` (`index.js:12425`, `:13380`; `lib/providers/openclaw-memory-embedding-adapters.js:56`), `OPENCLAW_CONFIG_PATH`, `NODE_TEST_CONTEXT` (`index.js:4524`) | — | — | — | Adapter | `Host.stateDir`, `Host.configPath` |

`api.*` usage counts (`grep -rho "api\.[a-zA-Z]*" index.js lib/ | sort | uniq -c`, re-run @ 89148f9): `api.logger` 341, `api.on` 30, `api.registerGatewayMethod` 14, `api.config` 14, `api.registerService` 11, `api.registerCli` 8, `api.registrationMode` 5, `api.registerMemoryPromptSupplement` 4, `api.session` 3, `api.lifecycle` 3, `api.runtime` 2 (+20 via `runtimeIfUsable`), `api.resolvePath` 2, `api.registerRuntimeLifecycle` 2, `api.registerMemoryCorpusSupplement` 2, `api.registerMemoryCapability` 2, `api.registerHttpRoute` 2, `api.registerEmbeddingProvider` 2, `api.registerControlUiDescriptor` 2, `api.registerCommand` 2, `api.pluginConfig` 2, `api.registerTool` 1. `api.openai`/`api.cohere` hits are URL literals, not API members (research §9). `grep -c -o "api\.register[a-zA-Z]*\|api\.on(" index.js` = **37** registration/hook sites in `index.js` alone.

---

## (b) Time budgets

| Budget | Value | file:line | Notes |
|---|---|---|---|
| Recall hook (host-facing) | **50 000 ms** | `index.js:13351` (`recallTimeoutMs + 5_000`) | `recallTimeoutMs` default 45 000 (`lib/runtime-scheduler.js:7`, normalized `:40`) |
| Recall soft budget (plugin config) | **35 000 ms** | `index.js:4711` (`recallCfg.softBudgetMs ?? 35_000`) | `softBudgetFallback` on unless `=== false` (`index.js:4712`) |
| Recall soft budget (scheduler-derived) | `max(1000, min(recallTimeoutMs-2000, recallTimeoutMs*0.5))` = **22 500 ms** at defaults | `lib/runtime-scheduler.js:392` | Two independent soft budgets exist; the pipeline uses the `index.js` one |
| Capture hook | **60 000 ms** | `index.js:11299` | scheduler `captureTimeoutMs` 60 000 (`lib/runtime-scheduler.js:8`) |
| Conversation reactivation | **50 ms** hard `Promise.race` | `index.js:12967-12969` (`crrCfg.timeoutMs ?? 50`) | tightest budget in the plugin; rejects `crr_timeout` |
| Embedding readiness probe | **10 000 ms**, cached **300 000 ms** | `lib/setup/memory-host-runtime.js:26`, `:25` | probe text `:24` |
| Embed request (remote) | **15 000 ms**, floor 1000 ms | `lib/providers/config-normalize.js:63`, `:70-73` | rationale `:65-68`: the OpenAI SDK default is ten minutes |
| Rerank | **5 000 ms** | `lib/providers/config-normalize.js:158` | enforced twice — provider `AbortController` (`lib/providers/reranker-cohere.js:50,74`) and a pipeline `Promise.race` (research embedding §2) |
| Host-facing embedding adapter | **60 000 ms** query / **120 000 ms** batch, 10 000 chars/text | `lib/providers/openclaw-memory-embedding-adapters.js:23-25` | Diverges from the 15 s plugin path — document both |
| Feature-cron RPC | **540 000 ms** | `lib/setup/feature-cron-plugin-runtime.js:20` | cron job itself `--timeout-seconds 600`, `--output-max-bytes 65536` (`scripts/setup-feature-crons.mjs:140-147`) |
| Native-capability probe | **30 000 ms** | `scripts/setup-feature-crons.mjs:48` | raised from 5 s; host CLI boot measured 11.6 s (`:41-47`) |
| Neo worker warm-up delay | **20 000 ms** after `gateway_start` | `index.js:5296`, `:5300-5306` | hook itself `{timeoutMs: 5_000}` |
| Deferred cron bootstrap delay | **90 000 ms** (native ready) / **0 ms** | `index.js:7066` | hook `{timeoutMs: 5_000 \| 30_000}` (`index.js:7069`) |
| Scoped-embedding IPC | response **120 000 ms**, request frame **10 000 ms** (1–60 s allowed) | `lib/providers/scoped-embedding-ipc.js:26-27` | see §d |
| Direct-provider LLM | **30 000 ms** | `lib/llm-call.js:3` | |
| Knowledge-update LLM | `resolveKnowledgeUpdateTimeoutMs(maxTokens)` | `index.js:12072`, `:12107` | value not a constant |

**Host-side defaults (OpenClaw @ `b9421f4`), for the harness to match or better:** `before_prompt_build` 15 000 ms, `before_agent_run`/`before_install`/`before_tool_call`/`before_agent_finalize`/`message_sending`/`reply_payload_sending`/`resolve_exec_env` 15 000 ms, `skill_proposal_evaluate` 120 000 ms (oc:`src/plugins/hooks.ts:138-155`); void hooks `agent_end` 30 000 ms, `before_compaction`/`after_compaction`/`skill_changed`/`skill_proposal_changed` 30 000 ms, `channel_pairing_requested` 2 000 ms, `gateway_stop` 5 000 ms (oc:`src/plugins/hooks.ts:117-137`). A plugin-supplied `timeoutMs` takes precedence over the default (oc:`src/plugins/hooks.ts:616-628`), so PLUR1BUS's 50 s / 60 s / 30 s values win. **A timed-out hook is logged and skipped, but the plugin's underlying work is not cancelled** (oc:`src/plugins/hooks.ts:110-112`) — the harness must instead cancel, because PLUR1BUS's own `manager.search` already drops the signal (§f.1).

Reconciliation of the "15 s abort" comment at `index.js:12311-12313`: the recall handler asks for 50 s and gets it at `b9421f4`; the **reply-outcome** handler at `index.js:12216` registers without options and therefore runs under the 15 000 ms default while sitting *ahead* of recall in the same hook chain. Whether 2026.8.2 honoured the plugin value is **unverified** (that host version was not read).

---

## (c) Identity and principal model

### c.1 What exists today

| Element | file:line @ 89148f9 | Semantics |
|---|---|---|
| Canonical context builder | `lib/memory-request-context.js:284-320` | `resolveMemoryRequestContext(commandCtx, {requireWorkspace, requireUser, workspaceAliases})`. Reads `agentId` (**required**), `workspaceId`, `workspaceKey`, `workspaceDir`, `userId ?? senderId`, `channel ?? provider`, `accountId ?? account_id`, `chatId`, `chatKind`, `sessionKey`, `sessionId`. Returns a **frozen** object. Pure — no host import |
| **User principal** | `lib/memory-request-context.js:302-304` | `userPrincipal = userId && channel && accountId ? "user:v1:" + sha256(JSON.stringify([channel, accountId, userId])) : ""`. All three required, or there is no user principal and the `user` pool is unreachable |
| Workspace principal | `lib/memory-request-context.js:27-28`, `:80-97`, `:105-108`, `:173` | `workspace:v1:<key>` or `workspace-dir:v1:<realpathSync(dir)>` |
| Physical pool keys | `lib/memory-request-context.js:37-39`, `:42-44` | `workspacePoolKey = "w-" + sha256(...).slice(0,62)`; `userPoolKey = "u-" + sha256(...).slice(0,62)` — these are on-disk directory names, so the hash is a **storage key** |
| ACL enforcement | `lib/acl-middleware.js:102-159`; scopes `:1-9` | `agent-private` (default) / `workspace` / `user`. `user` requires the requester's `ctx.userPrincipal` to match `/^user:v1:[a-f0-9]{64}$/` **and** equal `ownerUserId`. Stable reason codes at `:110-159` |
| Critical-type default | `lib/acl-middleware.js:26-33`, `:161-180` | `person, beziehung, geburtstag, geld_konto, gesundheit, zugang_passwort` default to `agent-private` (German type names) |
| Tool-context resolution | `lib/memory-request-context.js:781-793` | Field-name map: `requesterSenderId`→userId, `messageChannel`→channel, `agentAccountId`→accountId, `chatId ?? deliveryContext.chatId ?? deliveryContext.to.split(":").at(-1)` |
| **Hook-context resolution — the 6-step proof** | `lib/memory-request-context.js:1259-1418` | `resolveHostHookMemoryContext(hookCtx, {getSessionEntry, workspaceAliases, accountTopology, turnRoutes, routingCapability, logger})`. (1) reject non-user triggers `["cron","heartbeat","background","manual"]` → `non_user_trigger` (`:1274-1276`); (2) `hook` — the hook must carry `runId`, `sessionId`, `senderId`, `chatId` and a matching agent id (`:1317-1404`); (3) `session_entry` — a host session-entry read (`:1332-1342`, sync SQLite, 10–90 ms observed, warn ≥ 1000 ms); (4) `entry` — entry `sessionId`, provider and account must agree with the hook; (5) `target` — the delivery-target peer id must equal `chatId`; (6) `ticket` — `turnRoutes.claimForPrompt` (below). **Any failure returns the unauthenticated `safeHookBase`, never throws** (`:1405-1417`) |
| Turn-route ticket | `lib/memory-request-context.js:1377-1393`, proof modes `:1372-1376` | Ticket must match `agentId, sessionKey, provider, accountId, chatId, peerKind, threadId` and `ticket.senderProof === sha256(senderId)`, plus `runId` when `proofMode === "turn-run"`. Modes `account-session` / `single-account` / `turn-run`. Minted by `reply_dispatch` (`index.js:12259-12275`), cleared on `agent_end` (`index.js:12278-12283`) |
| Headless / webchat route | `lib/memory-request-context.js:1291-1315` | With no bound transport identity, `runId`+`sessionId` plus a parseable `agent:<id>:<rest>` key yields an agent+workspace-only context — **no user principal** |
| Account topology | `lib/memory-request-context.js:804-815` | `buildMemoryAccountTopology(cfg)` walks `cfg.channels[provider].accounts` + `defaultAccount`, always includes `"default"`, marks a provider `ambiguous` when >1 account |
| Channel binding discovery | `lib/memory-request-context.js:244-273` | Walks `hostConfig.channels[*].accounts[*]` and `bindings[*].match.{channel,accountId}` — hard dependency on OpenClaw's config shape |
| **Hard-coded channel list** | `lib/memory-request-context.js:24-25` | `SUPPORTED_PEER_KINDS = {direct, dm, group, channel}`; `SUPPORTED_ROUTE_PROVIDERS = {telegram, discord, slack, mattermost}`. WebChat is excluded from the trusted-command allowlist (`KNOWN-ISSUES.md:13`) |
| **No `agent_context` object** | `index.js:7195-7201`; `lib/memory-request-context.js:1274` | Cron/sub-agent/background turns are inferred from `context.trigger`, `commandCtx.channel/origin/source/kind`, a `sessionKey` regex `/^agent:[^:]+:cron(?::\|$)/`, and `isBackgroundTurn(event, ctx)` / `shouldSkipAutoCaptureForInternalTurn` / `shouldSkipAutoRecallForInternalTurn` |
| Incognito classification | `index.js:10363-10375`; capability `lib/memory-request-context.js:339-342` | `classifyHostIncognitoSession(sessionKey)` wraps SDK `isIncognitoSessionKey`; fail-closed on missing/failed/non-boolean classifier. A turn with **no** session key is captured anyway with a one-shot warning (`index.js:10378-10382`) |

Summary: the durable authorization key is **(canonical `agentId`, workspace principal, `user:v1:sha256([channel, accountId, userId])`)**, with `chatId`/`chatKind` for ACL and confirmation binding. The session key is parsed, never trusted alone. There is no first-class host principal: PLUR1BUS reconstructs one from six loosely-typed hook fields, a session-entry read and its own ticket ledger. `docs/compatibility-openclaw.md:190`: *"OpenClaw session Owner is responsibility/display metadata, not authorization."*

### c.2 What the harness must supply instead

| Requirement | Replaces | Rationale |
|---|---|---|
| A **canonical `Principal`** delivered with every engine call — `{ agentId, workspace: WorkspacePrincipal, user?: UserPrincipal, channel: ChannelRef, chat: { id, kind }, trust: "proved" \| "inferred" }`, minted by the harness transport layer and defined in ADR-007 | `reply_dispatch` ticket + `getSessionEntry` + the 6-step chain + `openclaw/plugin-sdk/routing` (`lib/memory-request-context.js:1259-1418`, `index.js:12259-12283`) | The harness owns the transport, so identity is a fact at admission, not a reconstruction. The whole proof subsystem (~180 lines of `memory-request-context.js` plus 2 hooks) is deleted |
| `UserPrincipal` must remain **`user:v1:sha256([channel, accountId, userId])`** for existing stores, with a documented migration path if ADR-007 defines a different canonical user id | `lib/memory-request-context.js:302-304`, `:42-44` | The hash is the on-disk pool directory name; changing it without a migration orphans every `user`-scoped row (`lib/acl-middleware.js:139-159`) |
| An **open channel vocabulary** — `channel` as a registered identifier, not a 4-value literal, with WebChat, ACP/coding-CLI (D5) and cron as first-class values | `lib/memory-request-context.js:24-25`; `KNOWN-ISSUES.md:13` | D5 requires coding CLIs as agents; the closed list silently refuses commands from any new surface |
| Explicit **`agent_context`** — `{ origin: "user" \| "cron" \| "subagent" \| "heartbeat" \| "system", jobId?, parentRunId?, background: boolean }` passed into `Engine.recall`/`Engine.capture` | string matching at `index.js:7195-7201`, `lib/memory-request-context.js:1274`, `isBackgroundTurn` | Removes a session-key regex from the authorization path and makes subagent turns (D5) representable, which they are not today |
| **Case- and separator-normalized** workspace identity before hashing on Windows | `workspace-dir:v1:<canonicalDir>` (`lib/memory-request-context.js:173`) | `C:\Users\X` vs `c:\users\x` hash differently and split a workspace's memories in two (research Windows §4) |
| `logViolations` **on by default** with rotation | `lib/acl-middleware.js:183-215`, `:227-238` (default-off JSONL at `<workspaceDir>/.adaptive-learning/acl-audit.jsonl`) | Denials are invisible today |
| Preserve the **degraded mode**: unresolvable identity → agent-private scope, never an exception | `lib/memory-request-context.js:1405-1417` | Proven behaviour; keep it, but emit a typed warning the harness surfaces |

---

## (d) Embedding / rerank call sites, IPC protocol, single-dimension assumption

### d.1 Call sites and providers

| Item | file:line @ 89148f9 | Detail |
|---|---|---|
| Provider factory | `lib/providers/factory.js:9-27` | `local-transformers` → `LocalTransformersEmbeddingProvider`; else `OpenAIEmbeddingProvider` with `resolveApiKey(defaultEnv:"OPENAI_API_KEY")` |
| Reranker factory | `lib/providers/factory.js:29-67` | `disabled` \| `cohere` (optional local fallback) \| `local-transformers` (optional *different* local fallback; same model throws) |
| Recall query embedding | `lib/recall-pipeline.js:1530-1532` | `embeddings.embedQuery(queryForEmbed, embeddingContext)` with an `embed` fallback; one vector per recall (plus one for a refined query at `:1670`) |
| Rerank in pipeline | `lib/recall-pipeline.js:2021-2058`, timer `:1466` | Runs only when `reranker && boosted.length > 1`; invalid/empty index results and timeouts fall back to unreranked `topN`, adding a decision-trace guard `{name:"rerank", passed:false}` |
| Rerank call sites (plugin) | `index.js:4497`, `:11420`, `:12586` | — |
| Share re-embed | `lib/telegram-commands/memory-edit.js:508` | `embeddings.embed(card.text \|\| card.summary, { agentId: sourceAgent })` — a **fresh** embedding, not the stored vector |
| Host-facing adapters | `lib/providers/openclaw-memory-embedding-adapters.js:15-25`, `:204-209`, `:317-324` | Ids `plur1bus-openai`, `plur1bus-openai-compatible`, `plur1bus-e5-small`; `inputType === "query"` → `embedQuery`, else `embedPassage`; 60 s / 120 s / 10 000 chars |
| Embedding cache key | `lib/embedding-cache.js:57-58` | `provider \x00 model \x00 dimensions \x00 scopeId \x00 cacheVersion \x00 textHash` — includes dimensions, so it is safe under a dimension change; scope `agent` \| `shared` (`:67-71`) |
| Model/dimension catalog | `lib/providers/dimensions.js:1-29` | `text-embedding-3-small` 1536, `-3-large` 3072 (default), `ada-002` 1536, `intfloat/multilingual-e5-small` 384 (local default), `jina-embeddings-v3` 1024, `jina-v5-text-nano-retrieval` 768; Cohere `rerank-v3.5` (`:19`), local reranker `woxpas-ai/bge-reranker-v2-m3-onnx` (`:20`) |
| Pinned artifacts | `lib/providers/local-model-artifacts.js:18-113`; download `:255-256`; verify `:207-236`, `:290-315`; license gate `:458`, `:468` | Revision + per-file SHA-256 + byte size; URL `https://huggingface.co/${repository}/resolve/${revision}/${path}`; two Jina profiles are CC BY-NC 4.0 (`:50-51`, `:79-80`) and require `acceptNonCommercialLicense` |
| Host has no rerank concept | `KNOWN-ISSUES.md:56` | "OpenClaw has no reranking concept; PLUR1BUS supplies it entirely." Confirms D9: rerank is engine-owned, never host-delegable |

### d.2 Embedding-owner IPC protocol (`lib/providers/scoped-embedding-ipc.js`, 720 lines)

| Element | file:line @ 89148f9 | Value |
|---|---|---|
| Paths | `:263-287` | `<stateRoot>/control/embedding-ipc/{owner.sock, owner.token}`; `mkdirSync(..., {mode:0o700})` at `:265,277,278`, `chmodSync(dir,0o700)` at `:279` |
| Socket permissions | `:434-435` | `listenServer(server, paths.socketPath)` then `chmodSync(socketPath, 0o600)` |
| Token file | `:143-163` | `openSync(…, "wx", 0o600)` → `fsync` → `rename` → `chmodSync(tokenPath, 0o600)` (`:153`) |
| Ownership election | `:207-216` | Linux abstract socket `\0plur1bus-embedding-owner-v1-<sha256(dir)[0:40]>`; **every other platform** an exclusive loopback bind `127.0.0.1:49152 + (hex[0:4] % 16384)` (`:213-215`, constants `:28-29`), rationale `:210-212` |
| Envelope | `:61-92` | Exactly `{dimensions, fingerprintId, model, request, token}`; `request = {operation, texts}` with `operation ∈ {query, passage, batch}` (`:30`); non-`batch` requires exactly one text (`:89-91`); unknown fields rejected (`:66-68`) |
| Limits | `:24-27`, `:70-88` | `MAX_EMBED_TEXTS 64`, `MAX_EMBED_CHARS`/`MAX_EMBED_TOTAL_CHARS` enforced at `:75-88`, `MAX_REQUEST_BYTES 256 KiB`, `MAX_RESPONSE_BYTES 4 MiB`, `RESPONSE_TIMEOUT_MS 120_000`, `DEFAULT_REQUEST_FRAME_TIMEOUT_MS 10_000` |
| Auth + identity binding | `:376-390`, `:31` | 32-byte hex token compared with `timingSafeEqual`; model+dimensions must match (`scoped_embedding_identity_mismatch`); immutable fingerprint `embedding:v1:sha256:<64 hex>` (`FINGERPRINT_ID_RE`, `:31`) must match (`scoped_embedding_fingerprint_mismatch`) |
| Activation epochs | `:32-58` | A `Symbol.for("@cyb3rb1ade/plur1bus-memory/scoped-embedding-activation-epochs")` global map binds the owner to a reload generation |
| macOS guard | `:270-276` | Throws `scoped_embedding_socket_path_too_long` above 103 bytes — the precedent for a `win32` branch |
| Response | research embedding §2 | `{ok:true, vectors}` or `{ok:false, error:{code,message}}` |

**Harness mapping.** The protocol is transport-agnostic; only the address is not. `Engine.embedding.serve()` keeps the envelope verbatim and takes an `ipcAddress()` from a new `lib/platform.js`: abstract socket on Linux, filesystem socket on darwin/BSD, `\\.\pipe\plur1bus-embedding-<sha256(stateRoot)[0:32]>` on Windows with a pipe ACL instead of `chmod` (research Windows §4).

### d.3 Single-dimension assumption sites

| Site | file:line @ 89148f9 | What assumes one dimension |
|---|---|---|
| Config | `lib/providers/config-normalize.js:20-45`, `:86-91`, `:123-128` | Exactly one `dimensions` scalar per plugin config, validated against the model profile |
| **Pool construction** | `index.js:5594-5595` | `new MultiNamespacePool(namespaceLayout, vectorDim, AgentDbPool, api.logger)` and `new SharedMemoryPool(sharedBaseDir, vectorDim, AgentDbPool, api.logger)` — the *same scalar* for private, workspace and user tables |
| Pool field | `lib/multi-namespace-pool.js:210-212` | `constructor(layout, vectorDim, AgentDbPoolClass, logger)` → `this.vectorDim = vectorDim`, passed to every `MemoryDB` (also `:335-366`) |
| Per-DB construction | `index.js:1054`, `:1080`, `:2561`, `:2591`, `:2708` | one dimension per pool, not per DB |
| Zero-vector placeholder | `index.js:1701` | `vector: Array(this.vectorDim).fill(0)` |
| Schema guard | `lib/providers/dimension-guard.js:20-33`, `:35-40` | Reads `schema.fields.find(f => f.name === "vector").type.listSize`; `checkDimensionCompatibility` returns `ok \| mismatch \| no-existing-table \| unknown` |
| Share path | `lib/shared-memory.js:202`, `:206-207` | `isFiniteVector(vector, targetDb.vectorDim)` then an Arrow-schema equality check |
| Runtime validation | `lib/providers/embedding-openai.js:137-144` | First vector sets `_detectedDim`; a later mismatch throws "Embedding-Dimension-Mismatch…" |
| Query fusion | `lib/recall-pipeline.js:1530-1532`, `:1670` | One query vector used against every table; scores are raw `1/(1+distance)` and compared directly |

**Consequence for multi-identity recall (harness requirement).** Per research embedding §2: `vectorDim` must become a per-route value in `MultiNamespacePool`/`SharedMemoryPool` and per-DB in `AgentDbPool`/`MemoryDB`; the pipeline needs one embedding per identity instead of one `queryVector` threaded through `hydrateGraphResults` (`lib/recall-pipeline.js:1995-2004`); fusion must move from raw distance to **rank fusion (RRF or similar)** because scores from different models are not comparable — today `minScore`/`forgetThreshold` default to 0.3, which "never filters" (`KNOWN-ISSUES.md:66-70`); the share path already re-embeds (`lib/telegram-commands/memory-edit.js:508`), so only the equality check at `lib/shared-memory.js:202-207` and the cache `scopeId` change.

---

## (e) Corrections to the original commission

| # | Statement in the commission / brief §2 | Reality | Evidence |
|---|---|---|---|
| e.1 | `agent_turn_prepare` is a hook PLUR1BUS uses | **PLUR1BUS registers no `agent_turn_prepare` handler at 89148f9.** `grep -rn agent_turn_prepare index.js lib/` returns nothing. It is listed only in a stale audit. The hook *name* does exist host-side | empty grep; stale claim `OPENCLAW_SDK_COMPAT_AUDIT.md:30`; host union oc:`src/plugins/hook-types.ts:107` |
| e.2 | "session/shutdown hooks" | **No `session_start` / `session_end` registration exists** (`grep -rn "session_start\|session_end" index.js lib/` → empty). Shutdown is `gateway_stop` + `registerRuntimeLifecycle` + `registerService`. The host *does* define both names | empty grep; `lib/runtime-shutdown.js:232-236`, `:300-308`; host union oc:`src/plugins/hook-types.ts:129-130` |
| e.3 | "compaction checkpoint" hook | **PLUR1BUS registers no compaction hook.** Compaction enters only as the read `event.compactedAt \|\| ctx.compactedAt`, which drives reactivation recall. The host defines `before_compaction`/`after_compaction` with a 30 000 ms default, so a harness-side checkpoint is available and PLUR1BUS simply does not use it | `index.js:12959`; `docs/compatibility-openclaw.md:186` ("PLUR1BUS supplies no file-memory flush plan because conversation capture is handled by typed hooks"); host oc:`src/plugins/hook-types.ts:116-117`, oc:`src/plugins/hooks.ts:120-131` |
| e.4 | "session entry form" registration | **No such registration exists.** `grep` for `registerSessionEntry`/`sessionEntryForm`/`entryForm` finds nothing. What exists is a **read** of the host session entry | research §7 grep; reads at `index.js:12322`, `:9297`, `:9314`, `:9333`; workspace fields `index.js:9327-9331` |
| e.5 | PLUR1BUS manages MEMORY.md / USER.md | **PLUR1BUS never reads or writes MEMORY.md or USER.md.** It only *authorizes* the host to inject them via `classifyWorkspaceMemoryPaths`. The only MEMORY.md code paths are a legacy-migration comment and a reindexer that *reads* a host-curated file | `lib/setup/workspace-memory-provenance.js:1-17`, `:35-60`; `lib/setup/memory-host-runtime.js:262-269`; `lib/promoted-memory-reindex.js:34,42,78,123-125` (read-only discovery); `lib/memory-dynamics.js:72,89` (migration comment). PLUR1BUS's own durable file is `memory/KNOWLEDGE.md` (`index.js:3921`) and its diary target is `DREAMS.md` (`lib/dreaming/dream-diary.js:27`) |
| e.6 | `OPENCLAW_SDK_COMPAT_AUDIT.md` describes the current contract | **Stale (2026-06-02, v6 era).** Contradicts the code on at least three points | `:30` lists `agent_turn_prepare` (unregistered); `:39` lists `registerMemoryEmbeddingProvider`, whereas the code uses `registerEmbeddingProvider` (`lib/providers/openclaw-memory-embedding-adapters.js:317`) and `docs/compatibility-openclaw.md:35-36` says "The retired memory-specific registrar is not used"; `:211` claims "PLUR1BUS hat keine UI-Komponenten" — false, `lib/setup/control-ui-plugin-runtime.js` is 1426 lines including a full renderer |
| e.7 | Brief D4: "own scheduler (never host cron)" | Confirmed as a *change*, not a description: today **all 11 specs are provisioned into the host cron** via `openclaw cron add` and dispatched back through Gateway RPC | `lib/setup/feature-cron-plan.js:25-170`; `scripts/setup-feature-crons.mjs:140-168`; `scripts/run-feature-cron.mjs:1-20` |
| e.8 | Research note: "`scripts/*.mjs` + `*.sh` = 6 607 lines, 30 files" | **Minor correction.** `scripts/*.mjs` = 27 files / **6 607** lines; `scripts/*.sh` = 4 files / **2 655** lines; plus `scripts/lib/` (1 090 lines). Total under `scripts/` = **10 352** | `wc -l` @ 89148f9 |
| e.9 | Brief §2: "`runtime.llm.complete` … time budget" implies a documented host budget | There is no host budget on `runtime.llm.complete`; bounding is entirely PLUR1BUS's (`bounded.waitFor` + `route.timeoutMs`), with a 30 s default only on the direct-provider path | `lib/llm-router.js:412-450`; `lib/llm-call.js:3` |

---

## (f) Defects worth fixing during extraction rather than porting

| # | Defect | file:line @ 89148f9 | Why it is a defect | Fix in the extraction |
|---|---|---|---|---|
| f.1 | **Abort signal accepted and dropped** in the memory-slot search | `lib/setup/memory-host-runtime.js:170-172` (comment), `:172` (`recall({…, signal: opts?.signal ?? null})` with no consumer downstream) | The host can abandon a 50 s recall while the pipeline keeps burning CPU and an embedding request; combined with oc:`src/plugins/hooks.ts:110-112` ("the plugin's underlying work is not cancelled") nothing ever stops it | Make `AbortSignal` a first-class parameter of `Engine.recall` and thread it to the embedder, the reranker and every LanceDB call. Gate: a contract test that aborts mid-recall and asserts the provider call was cancelled |
| f.2 | **`process.env.HOME` on Windows** | `lib/providers/openclaw-memory-embedding-adapters.js:56` — `process.env.OPENCLAW_HOME \|\| join(process.env.HOME \|\| ".", ".openclaw")` | `HOME` is unset on Windows, so the model cache silently lands in the CWD. The rest of the codebase already uses `os.homedir()` (`lib/db-adapter.js:38`, `lib/neo-arch.js:428-429`, `lib/obsidian/safe-paths.js:9-10`, …) | One-line change to `homedir()`; fold into the `lib/platform.js` PR |
| f.3 | **Run marked complete without a narrative** (pre-7.12.58) | `lib/dreaming/rem-dream.js:1327-1338` | A REM run with no model output was recorded `completed`, so every later night answered `already_processed` and no dream ever reached `DREAMS.md` (the comment names the concrete case: 241 memories, 59 s, no diary entry). Fixed only from 7.12.58, and only by leaving the run open plus a `logger.warn` | Replace the boolean `runs.json completed[runKey]` with a persisted run record `{job, partition, startedAt, durationMs, outcome, reason, counts}` written **before** any early return (D4). Gate: a test that a no-narrative run persists `outcome:"incomplete"` and is retried |
| f.4 | **PID-reuse lock fails open** | `lib/neo-arch.js:296-305` — `process.kill(pid, 0)`, `EPERM` treated as alive | A recycled PID reads as a live holder; aggressive PID reuse on Windows turns this into a stuck Neo lock | Store `{pid, startedAt}` and compare, or use a named mutex. Same PR as f.5 |
| f.5 | **Lock liveness is mtime-only** | `lib/job-lock.js:13` (`DEFAULT_STALE_MS = 10 min`), `:20-27`, `:29-37` | `openSync(path,"wx")` is atomic everywhere, but a crashed holder blocks the job for up to 10 minutes because there is no OS-level liveness | Keep the file open for the run (Windows releases on process death) or shorten `staleMs` and record a start timestamp |
| f.6 | **Two independent 5 s rerank timers** | `lib/providers/config-normalize.js:158`; `lib/providers/reranker-cohere.js:50,74`; `lib/recall-pipeline.js:1466` | The pipeline can abandon a rerank while the HTTP request is still in flight, wasting the call and the quota | Single timeout owner in `Engine.rerank()`; the provider takes the caller's signal |
| f.7 | **Setup script cannot fail** | `scripts/setup-feature-crons.mjs:13-17`; nine silent-skip branches (research crons §1) | "must NEVER fail an install… exits 0" means a completely unprovisioned dreaming subsystem is indistinguishable from a healthy one | Harness provisioning is in-process and returns a structured result; no exit-0 contract |
| f.8 | **Fail-closed probe *disables* existing jobs** | `scripts/setup-feature-crons.mjs:57-72` (throws when `--help` text lacks exact markers), disable path `lib/setup/feature-cron-plan.js:412`, `:480-504` | A slower or differently-worded host CLI silently renames and disables working jobs; the budget already had to be raised 5 s → 30 s because boot took 11.6 s | Deleted with the host-cron path. Nothing in the harness parses `--help` output |
| f.9 | **`chmod`-only secrets on Windows** (7 sites + 1 `fchmod`) | `lib/providers/scoped-embedding-ipc.js:153,279,435`; `lib/shared-memory-migration.js:216`; `lib/workspace-policy.js:98`; `lib/model-preparation/state-store.js:130`; `lib/reembedding/lance-backend.js:126`; `lib/reembedding/state-store.js:179`; also `lib/llm-result-cache.js:225` (injectable `chmodFile`) | On Windows `chmod` only touches the read-only bit, so token and state files are world-readable | One `securePath(path)` helper in `lib/platform.js`: `chmod` on POSIX, per-user SID ACL on Windows. `lib/llm-result-cache.js:225` already takes the function as a parameter — the pattern exists |
| f.10 | **Sensitive-share gate duplicated** | `lib/shared-memory.js:158-173`, `:200` and `lib/telegram-commands/memory-edit.js:465-482` | Two copies of the same refusal list will drift | Single engine policy module |
| f.11 | **ACL violation logging is default-off** | `lib/acl-middleware.js:227-238`; JSONL sink `:183-215` | Denials are invisible in production, which is how "memory silently missing" incidents stay unexplained | Default on, with rotation (see §c.2) |
| f.12 | **Byte-exact `commandArgv` comparison** | `lib/setup/feature-cron-native.js:68-77` (compare), argv build `:49-55` | On Windows a drive-letter-case or backslash difference makes every job re-plan as a migration on each gateway start — an idempotency break, not a crash | Deleted with the host-cron path; if the OpenClaw adapter keeps it, normalize before comparing |
| f.13 | **Four bash scripts on the install/ops path** | `scripts/install-memory-system.sh:1` (local **and remote-via-SSH**), `scripts/backup-snapshot.sh:1-11`, `scripts/restore-snapshot.sh:1-12`, `scripts/protect-plur1bus-deploy.sh:1-13` (runs from `/root/.openclaw/scripts/` on a `*/15` cron, with an untracked production twin) | POSIX-only, and one has a drifting untracked copy | Port to Node `.mjs`; 27 `.mjs` scripts already exist, so the pattern is there |
| f.14 | **fd-based directory routing is off on Windows** | `lib/directory-capability.js:29-46`, message `:21-27`; `/proc/self/fd` and `/dev/fd` aliases `:49-60` | Named-namespace routing simply does not run on Windows | Accept as a documented degraded mode for the first Windows release (research Windows §4 tier 2), or reimplement with `realpathSync` + dev/ino comparison |
| f.15 | **Symlink refusal misses Windows junctions** (17+ sites) | e.g. `lib/dreaming/dream-diary.js:120`; `lib/jobs/gc-job.js:34`; `lib/garbage-collector.js:38`; `lib/reembedding/lance-backend.js:106,333,364,373,379`; `lib/providers/scoped-embedding-ipc.js:168,408` | `isSymbolicLink()` is false for junctions and most reparse points | One `isUnsafeLink(stat, path)` helper in `lib/platform.js` |

**Unverified items carried forward** (from the research notes, not re-checked here): `lib/registry-lock.js`, `lib/fsync-atomic.js`/`lib/atomic-file.js` behaviour under Windows `rename`-onto-open-file; `appendDestructiveOpLog` body (`lib/sql-safety.js:192`); whether REM trend output feeds recall ranking; `@lancedb/lancedb` Windows support.
