# M1a owner gate — decisions before M1b

M1a is merged (`openclaw-plur1bus-memory#184`, `main` @ `01861add`, 2026-09-23). Evidence for every point: `docs/superpowers/sdd-archive-m1a/whole-branch-review.md`, the ledger `progress.md` next to it, and `bench/results/2026-09-22-recall-budget-probe.md` in the PLUR1BUS repo. Answer as `G1=a G2=b …`; "Recommended" is the controller's proposal, not a default that applies silently.

| # | Decision | Options | Recommended | Why |
|---|----------|---------|-------------|-----|
| G1 | Engine still reaches OpenClaw through `lib/` (transitive `openclaw/plugin-sdk/routing` via `lib/memory-request-context.js` default parameter) and reads `OPENCLAW_HOME`/`OPENCLAW_CONFIG_PATH` at 8 sites in `engine/**`. Neither lint sees it. | (a) close both in PR-06/PR-14 as planned; (b) add a transitive-import lint + `host.stateDir` sweep as a PR-03b before PR-04; (c) accept as permanent | **a** | PR-06 (host-neutral `lib/`) and PR-14 (adapter move) own exactly these files; a PR-03b would touch them twice. Add the transitive lint to PR-06's acceptance. |
| G2 | `securePath` on Windows: `icacls` missing → `{ applied:false, reason:"acl-tool-unavailable" }`, all nine callers ignore it; a non-ENOENT `icacls` failure rethrows, and `lib/providers/scoped-embedding-ipc.js` is the one call site outside a `try`. | (a) callers log a warning once on `applied:false`, non-ENOENT stays a throw; (b) swallow every `icacls` error into `applied:false`; (c) leave as is until Windows CI exists | **a** | Silent fail-open on secrets/IPC paths is the wrong default; swallowing all errors hides real ACL failures. One warning per process is cheap and testable. Goes into M1b's Windows-port PR. |
| G3 | `isUnsafeLink`, `ipcAddress`, `canonicalIdentityPath` have no call site; `isUnsafeLink`'s win32 branch false-positives on case-different spellings and junction ancestors. | (a) wire them in M1b behind a dedicated test gate (case-fold + junction fixtures) before the 17-site symlink sweep; (b) wire immediately in PR-04; (c) drop `isUnsafeLink` and keep `isSymbolicLink()` | **a** | Wiring without the fixtures ships the false positives; dropping loses the reparse-point coverage Windows needs. |
| G4 | `process.env.HOME` → `os.homedir()` (`lib/providers/openclaw-memory-embedding-adapters.js`) is a real behaviour change: with `HOME` unset the model cache moves from `./.openclaw` to the real home. | (a) acknowledge — intended, CHANGELOG-recorded; (b) revert to `HOME` with `homedir()` fallback only | **a** | POSIX with `HOME` set is identical; the old behaviour wrote the cache into the working directory on Windows, which was a bug. |
| G5 | `PLUR1BUS_TYPECHECK_OPTIONAL=1` makes `npm run lint` pass without running `tsc` — the frozen contract's only gate has an off switch. | (a) remove the switch; CI and local lint always run `tsc` (typescript is already a pinned optional dependency); (b) keep, but CI sets it to `0` explicitly; (c) keep as is | **a** | M1b writes the harness against `types/engine.d.ts`; a gate that can be skipped is not a gate. The only reason for the switch was an out-of-tree `typescript`, which no longer applies. |
| G6 | B6 recall budget. Measured (stub embedder, synthetic corpus, N=20 so p95 = max): ×1 worst p95 242 ms; ×5 `recall-truncated` p50 612 / p95 808 ms. Owner's "40/60" is still uninterpreted. | (a) set p50 ≤ 400 ms / p95 ≤ 600 ms per recall at ×1 corpus, re-measure with the real embedder in M1b before enforcing; (b) set 40 %/60 % of a turn budget (needs a turn budget first); (c) no budget until real-embedder numbers exist | **a** | Matches the plan's reading of "40/60" as 400/600 ms, is met today at ×1, and gives M1b's embedder work a target. ×5 numbers show where the truncation path needs work (PR-05 signal threading, budget cut). |
| G7 | Commit identity going forward. M1a history is rewritten to `Cyb3rb1ade`; the Anthropic `Co-Authored-By` / `Claude-Session` trailers remain in commit bodies. | (a) keep trailers; (b) drop trailers; (c) drop and strip from history | **a** | Trailers are plain text in the body, carry provenance to the session, and change no authorship. |
| G8 | Product findings surfaced by the golden corpus, none fixed in M1a: dead `recall.globalInjectMaxChars` (17 000 unreachable; memories block capped at 12 000); `applyGlobalInjectBudget` cuts mid-attribute → malformed XML in the truncation path; `recall-over-budget` scenario misnomer; suite depends on `--test-concurrency=1`. | (a) fold all four into PR-05 (recall signal/budget work) with a new golden scenario per fix; (b) separate bugfix PR on `main` before M1b; (c) backlog | **b** | Malformed XML in injected context is user-visible today on OpenClaw hosts; a small fix PR on `main` ships it independently of the harness timeline and re-baselines the oracle once, cleanly. |

## Also open, no decision needed

- The remote branch `feat/engine-extraction-m1a` still exists on GitHub with the pre-rewrite (`Claude`-authored) commits. Delete with `git push origin --delete feat/engine-extraction-m1a` whenever convenient.
- Windows CI does not exist; every Windows claim in M1a is by inspection only.

## After the gate

M1b planning: brainstorming → `docs/superpowers/plans/…-m1b-….md` → subagent-driven execution. Scope per `docs/milestones.md`: PR-04…PR-09, PR-15, `createEngine()`, core daemon, IPC, session store, in-process embedding + reranking, dreaming scheduler, CLI skeleton, benchmarks.
