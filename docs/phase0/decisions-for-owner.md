# Decisions for the owner — releasing Phase 0 and starting M1

**Date:** 2026-09-22 · **For:** Christian (Cyb3rb1ade) · **Prepared by:** the independent Phase-0 review (`review-report.md`)

Answer with a numbered list of **yes / no / a value**. Where you say nothing, the stated recommendation is what gets built. Group **A** changes the original commission and needs a conscious yes. Group **B** blocks M1 or M2. Group **C** can wait until the milestone that needs it.

---

## Owner answers 2026-09-22

**Phase 0 is approved.** The owner answered this document in full on 2026-09-22 (German, verbatim below); every ADR moves from Proposed to Accepted the same day, with amendments D12–D15 where the answer changed this review's recommendation. Implementation starts with **M1a** (per B15).

| # | Owner's verbatim answer | Interpretation applied |
|---|---|---|
| A1 | "ja" | Confirmed as recommended — three Anthropic routes (Claude Code default / setup-token restricted / API key). No change to the row above. |
| A2 | "API (auch für Embeddings) Zusätzlich so wie bei Anthropic über die CLI / also grok und codex. Codex als 3. via oauth auf eigenes Risiko." | **Changes the plan (D13).** OpenAI and xAI each get Anthropic's three-route pattern: API key (allowed, default; OpenAI's also serves embeddings); vendor CLI as an unmodified spawned headless backend (Codex CLI / Grok CLI), allowed; and, for OpenAI/Codex only, ChatGPT/Codex OAuth as a third route, restricted, opt-in, "at your own risk." xAI's OAuth route is dropped — imported only as a disabled profile. Google is unaffected (stays prohibited). See row A2, ADR-005 "Amendment D13", `provider-matrix.md` §4. |
| A3 | "nein, es gibt ja auch persona-voice, etc. pp. es braucht ja auch eine Identity, User, etc." | **Reverses the recommendation (D14).** Keep `SOUL.md`; do not rename to `persona.md` — "persona" collides with PLUR1BUS's own persona-voice/persona-evolve vocabulary. The per-agent record also needs an identity file and a user file (OpenClaw's `IDENTITY.md`/`USER.md` kept as default names pending ADR-003's action items). See row A3/B2, ADR-003. |
| A4 | "yes" | Confirmed — the use-class wizard question (personal/non-commercial → Jina v5 Text Nano; commercial/unsure → Qwen3-Embedding-0.6B; non-interactive → E5-small) ships as designed. |
| A5 | "DailyNote_YYYY-MM-DD_HHMMSS.md (o.ä.), memories.md, dreaming.md, und knowledgepool.md brauchen wir alles" | **Changes the plan (D15).** Four per-agent curated files, not two: `DailyNote_YYYY-MM-DD_HHMMSS.md` (daily notes, naming may still be adjusted), `memories.md` (long-term memory / promotion target), `dreaming.md` (dream diary), `knowledgepool.md` (knowledge pool). Supersedes the `KNOWLEDGE.md`/`DREAMS.md` default; the OpenClaw adapter keeps writing those names for OpenClaw hosts. See row A5/B4, ADR-009. |
| A6 | "yes" | Confirmed — harness-owned scheduler replaces host cron entirely, as recommended. |
| A7 | "yes" | Confirmed — PR-08 (run-state semantics) and PR-10 (RRF ranking) both signed off as non-behaviour-neutral, owner-decided PRs. |
| A8 | "yes" | Confirmed — the four enumerated UI deviations from the OpenClaw look are frozen as recommended. |
| B1 | "2" | Confirmed — two repositories (engine, harness), as recommended (Q6). |
| B2 | "A3" | Same answer as A3 — keep `SOUL.md` (D14). |
| B3 | "Yes, JinaAI" | Confirmed — same use-class wizard as A4; Jina v5 Text Nano is the pre-selected default for personal/non-commercial use, Qwen3-Embedding-0.6B for commercial/unsure. |
| B4 | Same as A5 | Same answer as A5 — four memory files, not `KNOWLEDGE.md`/`DREAMS.md` (D15). |
| B5 | "no - 1st just measure it" | **Changes the plan.** No fixed dreaming cost cap in M1. M1 ships measurement (cost per phase per run in the `dream_run` ledger) and a hard circuit breaker on session count only (3 LLM sessions/sweep, unchanged). A cap is decided after real numbers exist. ADR-009, `assumptions.md` Q11 re-answered. |
| B6 | "40/60" | Coordinator's interpretation: **400 ms soft / 600 ms hard** recall budget. **Interpretation pending owner confirmation** — "40/60" is terse enough to admit other readings. The mandatory measurement of today's pipeline before PR-04 stays required regardless. ADR-002 §"Time budgets". |
| B7 | "yes" | Confirmed — package names `@cyb3rb1ade/plur1bus-engine`, `…-host-openclaw`, `…-control-ui`, `…-platform`; `@cyb3rb1ade/plur1bus-memory` stays the adapter. ADR-002 open question 1 answered. |
| B12 | "yes" | Confirmed — agent-owner timezone, private diary per agent, `0 */4 * * *` light-dream cadence only, retention as proposed (ledger 365 d / logs 30 d / diary forever). |
| B13 | "yes" | Confirmed — trace whether anything reads REM trend output before PR-07 (ADR-009 conflict C2), same trace for `emotion-refine`/`persona-evolve`/`discover-semantic-links`. |
| B14 | "yes" | Confirmed — dreaming gets a quality gate at M1 (promoted entries must beat a random control on next-week recall), not just an observability gate. |
| B15 | "yes" | Confirmed — **M1 splits into M1a and M1b.** M1a = PR-01…PR-03 + `Host` interface + golden-prefix corpus, owner gate; M1b = recall/capture/jobs + daemon + scheduler + CLI. `milestones.md` updated. |
| B16 | "in v0.1" | Confirmed — multi-identity recall ships in v0.1 (PR-10, in M2), matching ADR-006 Q4's recommendation. |
| B17 | "yes" | Confirmed — reranker default follows the same use-class split (Qwen3-Reranker-0.6B commercial / Jina Reranker v2 personal, BGE-v2-m3 kept as the permissive alternative); which pair wins by default is left to the M1 retrieval benchmark. |
| C | "Rest looks okay" | Every unlisted B-item (B8–B11, B18–B20) and every C-item keeps its stated default, including the full v0.1 scope (no M1–M3-only cut). |

---

## A. Confirmations that change the original commission

These eight recommendations depart from the commission of 2026-09-21. Each is evidence-driven and each is reversible only at a cost.

| # | Decision | Context (one line) | Recommendation | Discussed in |
|---|---|---|---|---|
| **A1** | **Answered 2026-09-22: three routes (Claude Code default / setup-token restricted / API key).** Anthropic subscription: shipped via Claude Code / Agent SDK backend under the user's own login as the default, plus a new opt-in setup-token route; no harness-native Claude.ai OAuth. Google subscription logins are still not shipped at all — not as opt-in, not behind a risk notice, not behind a hidden flag. | A fresh 2026-09-22 re-read of Anthropic's compliance page found a carve-out the first pass had missed: it forbids a harness-native Claude.ai login (implementing OAuth/PKCE/device-code, or reading `~/.claude/.credentials.json`/Keychain) but explicitly allows an end user to sign in to the *unmodified* Claude Code binary on a hosting platform — which is what the harness now does. The owner's own production OpenClaw runs Anthropic in token mode (verification-log V9), which is the evidence for the second route. Google's ToS still names "using OpenClaw with Gemini CLI OAuth" as a violation with no equivalent carve-out found — both re-verified verbatim on 2026-09-22 — and §6.3's own rule is "`prohibited` is never shipped". **Owner statement (2026-09-22, D12): "Also drei Wege: API, Restricted und Claude Code."** Accurate for the spawned-binary default plus the setup-token and API-key routes; the harness-native OAuth flow stays excluded regardless. | **Yes.** Ship three Anthropic routes: (1) **Claude Code / Agent SDK** as `allowed`, **default** — the harness spawns the unmodified `claude` binary (`--bare -p --input-format/--output-format stream-json --system-prompt <ours> --mcp-config <ours> --strict-mcp-config`) or `@anthropic-ai/claude-agent-sdk` under the user's own login and never sees the credential — for **both** ordinary chat turns and ADR-011 external-agent use; (2) **setup-token** as `restricted`, opt-in with a visible risk notice — the harness stores a user-pasted `claude setup-token` token and calls the Messages API with Claude Code's own request shape, never refreshes it, and this required narrowing §6.3's no-client-imitation rule (D12: client-shaping is permitted only for tokens the user obtained themselves, never a harness-implemented login or another client's credential store); (3) **API key**, `allowed`, unchanged. Keep a harness-native Claude.ai OAuth `prohibited`. For Google, still ship API keys plus the vendor's own unmodified CLI attached as an external ACP agent; show the disabled profile with its policy status, source link, check date and the alternative. Whether Gemini CLI (now Antigravity CLI `agy`, D40) has an analogous spawned-binary carve-out is **unverified** — carried as a re-check item, not assumed. Re-check every 90 days (first 2026-12-21); re-check Anthropic sooner on any announcement, since this exact policy already moved three times in 2026 (April ban, May-13 credit announcement, June-16 pause). | ADR-005 §Conflicts, "Amendment D12"; `provider-matrix.md` §4; ADR-011 Finding 2 |
| **A2** | **Owner answered 2026-09-22, verbatim: "API (auch für Embeddings) Zusätzlich so wie bei Anthropic über die CLI / also grok und codex. Codex als 3. via oauth auf eigenes Risiko."** OpenAI and xAI each get the **same three-route pattern as Anthropic (D12)**: (1) API key, `allowed`, default — OpenAI's key also serves embeddings; (2) the vendor's own CLI spawned unmodified as a headless backend — Codex CLI for OpenAI, Grok CLI ("Grok Build") for xAI — `allowed`; (3) **for OpenAI/Codex only**, ChatGPT/Codex OAuth as a third route, `restricted`, opt-in, "at your own risk." **xAI's OAuth route is dropped entirely** — the owner named "grok und codex" only for the CLI-backend route, not for OAuth — so xAI's production OAuth profile is imported only as a visible, disabled entry. Google Gemini-CLI (now Antigravity CLI `agy`, D40)/Code Assist/Antigravity OAuth is unaffected: still `prohibited` and unshipped, imported only as a disabled profile, unless the 90-day re-check finds the ToS changed. This is **owner decision D13**, superseding the draft in the row below. | The earlier draft in this row (flat `restricted` OAuth for both OpenAI and xAI) was this review's own recommendation, made before the owner specified the CLI-backend route. The owner's actual answer instead generalises Anthropic's three-tier shape (API / CLI-backend / restricted-OAuth) to OpenAI, and gives xAI only two of the three tiers. | Confirmed as **D13** (see the "Owner answers 2026-09-22" block above). ADR-005 (policy table, "Amendment D13"); `provider-matrix.md` §4; `assumptions.md` Q3; `brief.md` D13. | ADR-005 §Conflicts, "Amendment D12"/"Amendment D13", open question 1 |
| **A3** | **Owner answered 2026-09-22, verbatim: "nein, es gibt ja auch persona-voice, etc. pp. es braucht ja auch eine Identity, User, etc." — keep `SOUL.md`, do not rename to `persona.md`.** This review's proposed rename is declined: "persona" is already PLUR1BUS's own vocabulary (persona-voice, persona-evolve), so renaming the file *to* persona.md would create exactly the naming collision the rename was meant to avoid. The owner also flags that the agent record needs more than a persona file — an identity file and a user file too. | The original rationale (avoid Hermes product vocabulary) is still valid, but the proposed replacement name turned out to collide with the harness's *own* vocabulary, which the owner caught. `SOUL.md` has no such collision. | **No — keep `SOUL.md`.** Per-agent curated files are `SOUL.md` (persona) plus an identity file and a user file, with OpenClaw's own `IDENTITY.md`/`USER.md` kept as the default names pending ADR-003's action items fixing the exact names. This is **owner decision D14**. | ADR-003 §"The persona file" (revised); `assumptions.md` Q8 (re-answered); `brief.md` D14 |
| **A4** | **Default embedding model: a use-class question in the wizard, not one fixed default.** "Personal / non-commercial use?" → yes: **Jina v5 Text Nano** pre-selected (§6.2's original suggestion) with the explicit NC-licence confirmation; no / commercial / unsure: **Qwen3-Embedding-0.6B (Apache-2.0)** pre-selected; non-interactive install: **E5-small (MIT)** unless an explicit flag/env variable confirms the NC licence. | Owner question: "if the user agrees and uses it for their own purpose?" — CC BY-NC-4.0 does permit personal, non-commercial use, and the harness already has exactly the mechanism for that: the owner-only, audit-logged `acceptNonCommercialLicense` gate. §6.2 suggested Jina v5 Text Nano outright; all three Jina models in scope are CC BY-NC-4.0, so one fixed default either blocks commercial users (Jina) or drops §6.2's own suggestion entirely (Qwen3) — the use-class question keeps both, each honestly scoped to who it's for. | **Yes.** Ask the use-class question before pre-selecting anything; the wizard still shows the licence notice and still requires the explicit NC confirmation whenever Jina is chosen. Same use-class pattern applies to the reranker default (see B17). | ADR-006 §Conflicts, open question 1; `assumptions.md` Q9 |
| **A5** | **Owner answered 2026-09-22, verbatim: "DailyNote_YYYY-MM-DD_HHMMSS.md (o.ä.), memories.md, dreaming.md, und knowledgepool.md brauchen wir alles."** Supersedes the `KNOWLEDGE.md`/`DREAMS.md` default. Four per-agent curated files, all required: `DailyNote_YYYY-MM-DD_HHMMSS.md` (timestamped daily notes, light-sleep input — naming may still be adjusted, "o.ä."), `memories.md` (long-term memory, the deep-sleep promotion target), `dreaming.md` (the dream diary), `knowledgepool.md` (the curated knowledge corpus — receives `knowledge_update` content and REM/consolidation summaries). | The proposed `KNOWLEDGE.md`/`DREAMS.md` pair (this review's mapping of OpenClaw's `MEMORY.md` gap) covered promotions and the diary but not daily notes or a distinct long-term-memory file; the owner wants all four kept separate. The engine's OpenClaw adapter keeps writing its own `KNOWLEDGE.md`/`DREAMS.md` for OpenClaw hosts unchanged — only the harness host maps onto the new names. | **Superseded — four files, not two.** This is **owner decision D15**; the memories.md/knowledgepool.md split (consolidated facts vs. curated corpus) is recorded as a proposal in ADR-009, pending implementation experience. | ADR-009 (promotion target, file set — revised, proposal); `assumptions.md` Q10 (re-answered); `brief.md` D15 |
| **A6** | **A harness-owned scheduler replaces the host cron entirely.** | Today all 11 feature crons are provisioned into OpenClaw's cron by an external script that is contractually forbidden to fail and whose probe *disables* working jobs. This is why dreaming was invisible. | **Yes.** In-process `engine.jobs.run()`, a run ledger row written **before any early return**, per-phase schedule/enable/stagger, "run now", visible errors, a guaranteed diary. No `cron add`, no `--help` parsing, no shell scripts. | brief D4; ADR-009 |
| **A7** | **Two engine PRs are deliberately *not* behaviour-neutral** and need your signature before they merge: **PR-08** (run-state semantics — a run with no narrative stays open and is retried instead of being recorded `completed`) and **PR-10** (ranking — reciprocal-rank fusion replaces raw-distance comparison, needed for multi-identity recall). | Both are required by the brief (D4 observability; §6.2 multi-identity recall), so they are decisions, not risks to avoid. PR-10 additionally changes recall *order*, gated on a single-identity byte-identity test plus a recorded A/B. | **Yes to both**, and make the signature a Definition-of-Done item so neither can merge unsigned. | `engine-extraction.md` §c; ADR-002 C2; review-report S11 |
| **A8** | **Four enumerated deviations from the OpenClaw look**, against §9's "keine gewollten Abweichungen außer Wortmarke". | Your recorded critique — too dense, cluttered, generic, no settings search — cannot be met by a pixel-faithful copy. The control UI measurably contains zero search inputs and renders every setting as one long anchored page. | **Yes, and freeze the list at four:** (1) a `comfortable` density default with a `compact` switch, (2) routed pages instead of the single long page, (3) a settings search plus command palette, (4) the word mark "PLUR1BUS Harness". Everything else — token values, card/badge/banner patterns, dark default with light per OS, readiness tables — copied unchanged. Any further deviation needs a new ADR. | ADR-004 §Conflicts |

---

## B. Open questions that block M1 or M2

| # | Question | Recommendation / default | Blocks | Discussed in |
|---|---|---|---|---|
| **B1** | **Q6 — two repos or a monorepo** for engine and harness? | **Two repos.** The OpenClaw plugin has a live user base pinned to `openclaw >= 2026.8.1` and must ship host fixes without shipping harness changes. Flip to a monorepo if three consecutive milestones are blocked by cross-repo latency, or if M1 needs more than ~one coordinated two-repo PR per week. | End of Phase 0 | ADR-002 §"Two repos vs monorepo"; `engine-extraction.md` §e |
| **B2** | **Q8 — persona filename.** | Same decision as **A3** — reversed 2026-09-22 (D14): keep `SOUL.md`, do not rename; add identity and user files to the per-agent record. Listed here because PR-06 and the agent record depend on it. | M1 | ADR-003 |
| **B3** | **Q9 — default embedding model in the wizard.** | Same decision as **A4** — the wizard asks the personal/commercial use-class question and pre-selects Jina v5 Text Nano or Qwen3-Embedding-0.6B accordingly (E5-small non-interactive); needed at M1 because the answer fixes the warm-up model and the first store's embedding identity. | M1 | ADR-006 |
| **B4** | **Q10 — dreaming promotion target.** | Same decision as **A5** — answered 2026-09-22 (D15): four files (`DailyNote_*`, `memories.md`, `dreaming.md`, `knowledgepool.md`), not `KNOWLEDGE.md`/`DREAMS.md`. | M1 | ADR-009 |
| **B5** | **Q11 — dreaming cost cap per agent per day.** | **Owner answered 2026-09-22: "no - 1st just measure it."** No fixed cost cap in M1. M1 ships **measurement** instead — cost per phase per run recorded in the `dream_run` ledger — plus a **hard circuit breaker on session count only** (3 LLM sessions per sweep, unchanged from the guard design). A cost cap is decided once real numbers exist. | M1 | ADR-009 open question 1 (re-answered) |
| **B6** | **ADR-002 Q2 — the recall budget: soft 400 ms / hard 1 200 ms?** Today's values are 35 s soft / 50 s hard. | **Owner answered 2026-09-22, verbatim: "40/60."** Coordinator's interpretation: **400 ms soft / 600 ms hard** — read as shorthand for the two budget numbers in hundreds of milliseconds, tightening the hard budget from the original 1 200 ms proposal to 600 ms. **This interpretation is pending owner confirmation** — "40/60" is terse enough to admit other readings (e.g. a 40/60 split of some other total), and the mandatory measurement of today's pipeline before PR-01 stays required regardless of which reading is confirmed. | M1, **before PR-01** | ADR-002 §"Time budgets"; review-report R-B |
| **B7** | **ADR-002 Q1 — package names for the split.** Proposed: `@cyb3rb1ade/plur1bus-engine`, `…-host-openclaw`, `…-control-ui`, `…-platform`. And: does the published name `@cyb3rb1ade/plur1bus-memory` stay the **adapter** (existing OpenClaw installs unaffected) or become the **engine**? | **Owner confirmed 2026-09-22: "yes."** Package names as proposed; `@cyb3rb1ade/plur1bus-memory` stays the adapter, so no existing install breaks. Settled before PR-01. | M1, **before PR-01** | ADR-002 open question 1 (answered); `engine-extraction.md` §b.1 |
| **B8** | **Freeze the engine `.d.ts` before PR-01.** ADR-002 and `engine-extraction.md` currently sketch the same API **differently** in four places: `Principal.proof` vs `Principal.trust`, `TurnOrigin` as an object vs a string union + `AgentContext`, `capture()` non-blocking handle vs promise, `RecallResult.degraded` object vs boolean. | **Yes — one versioned `.d.ts` in the PLUR1BUS repo before PR-01**, taking `trust: "proved" \| "inferred"`, the separate `AgentContext`, the non-blocking capture handle and the structured `degraded` object. Both adapters are written against it. | M1, **before PR-01** | review-report S4; ADR-002 action item 2 |
| **B9** | **ADR-010 Q6 — pre-warm local models on daemon start?** | **Yes, pre-warm** (fast first turn, ~500 MB–1 GB RSS resident), with an opt-out for low-RAM machines. | M1 | ADR-010 |
| **B10** | **ADR-010 Q7 — are B1/B5/B6/B9 hard build gates from M1, or advisory until M3?** | **Hard from M1.** They are cheap, and a cache or cold-start regression caught by a test is worth an occasional blocked unrelated PR. | M1 | ADR-010 |
| **B11** | **ADR-009 Q2 — importance-accumulation triggering in M1, or cron-only in M1?** | **Cron-only in M1, importance triggering in M2.** The evidenced design is importance-triggered with cron as a floor, but the threshold is uncalibrated for our corpus (Generative Agents' 150 is a different corpus). If you defer it, drop criterion L6 from the M1 acceptance list rather than listing a criterion we may not meet. | M1 | ADR-009 |
| **B12** | **ADR-009 Q4/Q5/Q6/Q7 — timezone default, diary scope, light-dream cadence, retention.** | **Agent owner's timezone** (stored explicitly, never host-local-implicit); **private diary per agent**, with a shared workspace diary deferred; **`0 */4 * * *` cron only** for light dreaming (no additional post-session pass in M1); retention **ledger 365 days, per-run logs 30 days, diary forever**. | M1 | ADR-009 |
| **B13** | **Does REM keep a schedule if nothing reads its trend output?** The writers exist; **no reader was traced** in the recall pipeline. Tracing it is already a blocking action item. | **Trace it before PR-07.** If there is no reader, either wire trends into the recall ranker as part of REM's definition of done, or drop REM's schedule and drop it from the M1 A1 acceptance criterion. Do the same trace for `emotion-refine`, `persona-evolve` and `discover-semantic-links`, whose phase mapping rests on the same assumption. | M1 | ADR-009 C2; review-report S8, R-F |
| **B14** | **Should the harness add a dreaming *quality* gate at M1, not just an observability gate?** A1–A8 all pass on a scheduler that reliably promotes worthless material. | **Yes — add one.** Proposal: promoted entries must be measurably more likely to be recalled in the following week than a random control drawn from the candidate table. Without it, "dreaming demonstrably works" (D4) means only "dreaming demonstrably runs". | M1 | review-report R-E |
| **B15** | **Should M1 be split in two?** It carries ten engine PRs (three of them large), a core daemon, an IPC layer, a session store, an in-process embedding owner, a full dreaming scheduler and a benchmark harness, at 45–70 agent-days. | **Owner confirmed 2026-09-22: "yes."** **M1a** = PR-01…03 + the `Host` interface + the golden-prefix corpus, with an owner gate; **M1b** = recall/capture/jobs + daemon + scheduler + CLI. The boundary is where a mis-drawn extraction boundary would otherwise be discovered three PRs later. `docs/milestones.md` re-cut accordingly. | M1 | review-report R-A; `milestones.md` (updated) |
| **B16** | **ADR-006 Q4 — multi-identity recall in v0.1 or v0.2?** | **v0.1 (PR-10, in M2)** — §12's own M2 acceptance already demands two agents with different identities in parallel. If you defer it, that acceptance criterion moves with it and the interim rule is "per-store identities allowed, one identity per recall". | M2 | ADR-006 |
| **B17** | **ADR-006 Q2 — default reranker: BGE-v2-m3 via a third-party ONNX export, or Qwen3-Reranker-0.6B via `onnx-community`, and how does the personal/commercial use-class question (A4) apply here?** | BGE-v2-m3 is Apache-2.0 regardless of use class, so unlike the embedding default it need not be gated: personal use → **Jina Reranker v2** (CC BY-NC-4.0, needs the same NC confirmation as Jina embeddings) or **BGE-v2-m3**; commercial/unsure → **BGE-v2-m3** or **Qwen3-Reranker-0.6B** (both Apache-2.0). Because BGE is permissive anyway, BGE-v2-m3 can be the single default for both use classes if its quality is comparable to Qwen3-Reranker-0.6B — but that comparison has no source in Phase 0 research, so it is not asserted here and is instead **to be decided by the M1 retrieval benchmark**. Either way, ship the offline mirror bundle so an install never depends on a re-export repo staying online, and keep Qwen3-Reranker-0.6B as the permissive alternative regardless of which one the benchmark favours. | M2 | ADR-006 |
| **B18** | **ADR-010 Q1/Q2 — Anthropic 1-hour cache TTL (2.0× write), and whether a long session gets one deliberate memory-snapshot refresh.** | **Enable the 1-hour TTL automatically when the scheduler projects ≥ 3 reads in the window**; **one snapshot refresh** for sessions past 2 h or 50 turns, paying one full cache write to avoid a badly stale snapshot. | M2 | ADR-010 |
| **B19** | **ADR-005 Q3 — encrypted-file secret fallback: operator passphrase at daemon start, or a machine-bound key file?** | **Machine-bound key file with restrictive ACLs** as the default (survives reboot, which a headless server needs), with the passphrase mode available for higher-assurance installs. | M2 | ADR-005 |
| **B20** | **ADR-005 Q5 — per-user provider credentials (a Member brings their own key) in v0.1, or Owner/Admin-provisioned only?** | **Owner/Admin-provisioned only in v0.1.** Per-user credentials change the pool and RBAC model, not the flows, and can be added later without redesign. | M2 | ADR-005 |

---

## C. Later questions, by milestone

Answer these when the milestone starts; the stated default applies until then.

- **M3 (API, roles, UI).** **Q5** role model — five roles as presets with a "simple mode" (recommended; collapsing later is a preset edit, splitting later is a migration with a security window). ADR-004 Q1 framework — Preact+Signals, confirmed after a two-day measured spike. ADR-004 Q2 density default — `comfortable`. ADR-004 Q3 / ADR-001 Q5 (**the same question, asked twice**) — built-in TLS in v0.1, or reverse-proxy-only with documentation; default reverse-proxy-only. ADR-004 Q5 — does the logo package also cover favicon/monochrome/dark variants. ADR-007 Q2–Q6 — 2FA required for Owner/Admin; break-glass notifies immediately; unlink hides old-identity memories (with a one-click back-fill offered); OIDC deferred past M3; hard delete leaves a content-free tombstone.
- **M4 (channels).** **Q4** — confirm "one agent, many bot connections" for M4 with mention-routing no earlier than M6, gated on `AgentScope`. Note that `milestones.md` §7 currently lists multi-agent routing as out of scope for v0.1 entirely — say which of the two you mean. ADR-003 Q2 — a fifth per-chat precedence layer (default: no). ADR-003 Q3 — group capture default `provenance-only` (default: yes).
- **M5 (collaboration).** ADR-003 Q4 — external coding agents get no PLUR1BUS store of their own (default). ADR-003 Q5 — delegation depth 1 (default).
- **M6 (protocols, coding agents).** **Q7** — the M6 CLI set: Claude Code + Codex + Goose as gated acceptance, Gemini CLI (now Antigravity CLI `agy`, D40) best-effort; everything else attachable but "community-tested, unverified". ADR-008 Q1–Q6 — drop A2A 0.3 back-compat; push notifications off; MCP server exposes nothing until an operator enables each group; memory tools over MCP **read-only**; ignore ACP v2 until stable; gRPC deferred. ADR-011 Q2–Q6 — Tier-3 PTY deferred until a CLI needs it; external agents reachable only via `delegate_task` (not bound directly to a channel); budget in wall-clock + turns where the CLI reports no usage; keep attachment offered for prohibited-login vendors, since running their own binary is the compliant act.
- **M7 (importers).** ADR-007 Q4 — unlink semantics, which also decides whether import offers a v1→v2 back-fill.
- **M8 (release).** **Q2** — macOS x64 required or best-effort; default best-effort, because `@lancedb/lancedb` publishes no `darwin-x64` package at all and "required" means owning a Rust source build in CI. **ADR-001 Q3 no longer needs asking** — `node-pty` 1.1.0 does ship a complete `win32-arm64` prebuild (confirmed by tarball inspection in `platform-matrix.md` §5b); ADR-001 and ADR-011 still say "unverified" and should be corrected. ADR-001 Q4 — ship an ACP attachment recipe for an existing Hermes installation in v0.1, or defer.
- **Scope, whenever you want to answer it.** The plan totals **211–324 agent-days** across nine subsystems, four channels, three protocols and five platforms, gated so that v0.1.0 ships only when *everything* is done. The option nobody has put to you is a **v0.1 of M1–M3 plus one channel**, with M4–M7 as v0.2. Worth a yes or no now rather than at M6.
