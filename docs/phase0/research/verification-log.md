# Hands-on verification log (Phase 0)

Results of checks run directly rather than read from documentation. Each entry: what was run, where, result, date.

| ID | Check | Environment | Result | Date |
|----|-------|-------------|--------|------|
| V1 | `node:sqlite` FTS5 availability: `CREATE VIRTUAL TABLE t USING fts5(content)`, insert, `MATCH` + `bm25()` query, `PRAGMA compile_options` | macOS arm64 (owner's machine), Node v26.8.2 (Homebrew) | **OK.** SQLite 3.53.4; compile options include `ENABLE_FTS3, ENABLE_FTS3_PARENTHESIS, ENABLE_FTS5, ENABLE_MATH_FUNCTIONS, ENABLE_RTREE`. bm25 ranking works. | 2026-09-22 |
| V2 | Same as V1 | Linux x64 container, Node v22.22.2 | **OK.** SQLite 3.51.2, `ENABLE_FTS5`. | 2026-09-22 |
| V3 | `gh` CLI availability and auth on the owner's machine for repo creation | macOS arm64 | Logged in as Cyb3rb1ade with `repo` scope; `gh repo create Cyb3rb1ade/PLUR1BUS-Harness --public --license mit` succeeded. | 2026-09-22 |
| V4 | Toolchain on the owner's machine | macOS arm64 | node v26.8.2, pnpm 10.32.1, git 2.55.0 | 2026-09-22 |
| V5 | Fetched [code.claude.com/docs/en/legal-and-compliance](https://code.claude.com/docs/en/legal-and-compliance), sections "Authentication and credential use" and "Can customers offer Claude Code in their products?", read verbatim | Web fetch | **Confirmed.** A harness-native Claude.ai OAuth flow, or collecting/storing/intermediating a Claude.ai credential or session token, stays prohibited, verbatim: *"Anthropic does not permit third-party developers to offer Claude.ai login into their own applications, or to route requests through Free, Pro, or Max plan credentials on behalf of their users. Moreover, developers may not collect, store, or intermediate Claude.ai credentials or session tokens — sign-in to a Claude account must complete through Anthropic's own flow."* But the same page separately and explicitly permits an end user signing in to the **unmodified** Claude Code binary on a hosting platform, verbatim: *"Nor does it prevent an end user from signing in to the unmodified Claude Code binary with their own Claude subscription, including where a platform hosts Claude Code as described under Can customers offer Claude Code in their products? above."* That section's conditions: the binary must not be modified, no authentication method may be removed or disabled, nobody may pay for/resell/intermediate another user's usage, and each end user authenticates with their own credentials. This is the basis for ADR-005's new "Anthropic via Claude Code / Agent SDK" profile (`policy_status: allowed`) | 2026-09-22 |
| V6 | Fetched [support.claude.com/en/articles/15036540-use-the-claude-agent-sdk-with-your-claude-plan](https://support.claude.com/en/articles/15036540-use-the-claude-agent-sdk-with-your-claude-plan) | Web fetch | **Confirmed, now paused.** Article (published 2026-06-16) documents a monthly "Agent SDK credit" announced 2026-05-13 — Pro $20 / Max 5x $100 / Max 20x $200 / Team Standard $20 / Team Premium $100 / Enterprise $20–$200 — covering both "Claude Agent SDK usage in your own projects (Python or TypeScript)" and "Third-party apps that authenticate with your Claude subscription through the Agent SDK." The article is now headed *"We're pausing the changes to Claude Agent SDK usage described below"* — the credit model exists as documented but is not currently active | 2026-09-22 |
| V7 | Fetched [the-decoder.com: "Anthropic backs off unpopular billing overhaul as price war with OpenAI looms"](https://the-decoder.com/anthropic-backs-off-unpopular-billing-overhaul-as-price-war-with-openai-looms/) (2026-06-16) | Web fetch, secondary source | **Confirmed.** Anthropic paused the June-15 billing change with the statement *"Nothing changes for now"*: the Agent SDK, `claude -p` and third-party applications continue drawing from regular subscription limits. Article references the April-2026 ban of third-party tools (e.g. OpenClaw) from subscription limits as the prior state this reverses | 2026-09-22 |
| V8 | Fetched [docs.openclaw.ai/providers/anthropic](https://docs.openclaw.ai/providers/anthropic) | Web fetch | **Confirmed.** OpenClaw documents two Anthropic routes: an API key, and "Claude CLI," which reuses the existing Claude Code login via the installed executable, verbatim: *"Claude Code owns its existing login and subscription; OpenClaw does not persist or refresh that login."* and *"For shared production automation, use an Anthropic API key instead of Claude CLI."* This matches the harness's own never-persist/never-refresh design for the same profile (ADR-005, ADR-011) | 2026-09-22 |

Consequences:
- V1/V2: session storage and lexical search can use `node:sqlite` with FTS5 on Node ≥ 22.22 / 24 / 26 without a native add-on; `better-sqlite3` is not required. Windows arm64 and Linux arm64 still to be confirmed in CI (Node's official binaries bundle the same SQLite build, so no difference is expected).
- Not yet verified hands-on: `onnxruntime-node` and `@lancedb/lancedb` load on Windows arm64; `node-pty` prebuild on Windows arm64. Planned for the M1/M2 CI matrix.
- V5–V8: together these justify ADR-005's 2026-09-22 amendment shipping "Anthropic via Claude Code / Agent SDK" as `allowed` while keeping a harness-native Claude.ai login `prohibited`. They also show Anthropic changed this exact area of policy three times within 2026 (an April ban, the V6/V7 May-13 credit announcement, and its June-16 pause) — grounds for treating any further Anthropic announcement as an out-of-cycle re-check trigger, not just the standing 90-day cadence. No equivalent statement was found for Google's Gemini CLI; that remains an open, unverified item rather than an assumption.
