# SDD ledger — plan: docs/superpowers/plans/2026-09-22-m1a-engine-extraction.md

Worktree: /home/claude/work/plur1bus-m1a · branch feat/engine-extraction-m1a · base 89148f9f (main)
Spec: /home/claude/PLUR1BUS-Harness/docs/engine-extraction.md, ADR-002, host-contract.md, milestones.md (M1a), decisions-for-owner.md
Node: /home/claude/.node24/bin/node v24.21.0 · baseline: 5076 tests, 5071 pass, exactly 2 known host-dependent failures

## Pre-flight conflict scan (2026-09-22)

| Pair / task | Shared thing | Produces vs consumes | Finding |
|---|---|---|---|
| T1 ↔ T2 | `runScenario`, `SCENARIOS`, oracle files | T1 produces driver+oracle; T2 consumes | consistent |
| T1 ↔ T19 | `tests/helpers/golden-prefix-driver.js` | T19 adds `{ freezeClock }` option to `runScenario` | additive; T2's byte-identity test must keep default freeze=true — check in T19 review |
| T1,T3,T9,T10,T20 ↔ package.json | `scripts.lint` chain, `files` | each appends | sequential, no clash; T20 adds docs to files |
| T3 ↔ T4/T6 | `PlatformCapabilities`, `HostServices` types | T3 freezes; T4/T6 implement | T3 must land before T4 — order holds |
| T4 ↔ T5 | `securePath` | T4 produces; T5 routes 8 sites | consistent; RF2 (socket path) tested in T4 |
| T6 ↔ T7/T8 | `createHostServices(api)` → `host` at index.js:4445 | T6 produces; T7/T8 consume | logger normalised to 4 total methods (RF1) — required before T7 |
| T7/T8/T11–T18 ↔ index.js | line ranges | every move shifts later line numbers | plan says re-derive by Grep, never edit by line number — carried into every dispatch |
| T9 allowlist ↔ T10+ | `api.` outside adapter forbidden | T11–T18 create adapter/** (allowed) and engine/** (must not reference api) | consistent with GC8 |
| T15 ↔ T16 | `runPlur1busCommand` binding, thunks for `runMemoryCommand` etc. | T15 keeps binding name at "line 7255" (will drift); T16 returns the six bodies | T16 depends on T15 thunk design — carry the thunk list into T16 dispatch |
| T13 ↔ golden | `assemble-prompt-context` | T13 is the riskiest move; gate = golden byte-identity | ok |
| GC3 (oracle write-once) ↔ T1 | oracle | T1 writes on unmodified branch | ok — T1 is first |
| Self-consistency | T14 pre-commit fixes meta-reflection counters before move | own commit first | ok |

Scan verdict: no conflicts requiring a ruling before Task 1. Ruling: line numbers in briefs are advisory after Task 7 — implementers re-derive with Grep (plan §"Repository" already mandates this) — cost if wrong: an edit at a stale line, caught by suite/golden gate.

## Progress
Task 1: implemented 784cf625..050cbfe6 (7 oracle scenarios). Ruling: recall-truncated uses globalInjectMaxChars=11 000 because the memories block is hard-capped at 12 000 by truncateMemoryContext (lib/relevant-memory-context.js:67,261-262) so 17 000 never fires — finding "outer inject budget is dead config for the recall path" ledgered for M1b/ADR-002, not fixed in M1a (behaviour-neutral) — cost if wrong: none in M1a. Ruling: driver pins KNOWLEDGE.md mtime via utimesSync to FROZEN_NOW (canonical hits take age from mtime) — cost if wrong: an oracle that depends on a pinned mtime, which is what we want. Note: scenario `recall-over-budget` is misnamed (never exceeds budget); left as-is (oracle append-only). Trailer on 784cf625 says Claude Opus 5 — left.
Task 1: fix round 1/5 (2 addressed, 0 open — globals restored on early throw; embedder stub sealed at _embedBatchForPurpose + _computeBatch thrower; commits 050cbfe6..1912d691)
Task 1: minor (deferred): scenario `recall-over-budget` is a misnomer (oracle append-only); product findings for post-M1a: (a) recall.globalInjectMaxChars 17 000 is dead config on the recall path (memories block pre-capped at 12 000, lib/relevant-memory-context.js:67,262); (b) applyGlobalInjectBudget cuts mid-attribute → malformed XML in recall-truncated oracle; (c) recall-truncated ordering rests on a LanceDB tie-break (identical vectors + createdAt) — keep --test-concurrency=1; (d) hooks.at(-1) selects the recall hook positionally (plan-mandated).
Task 1: complete (commits 89148f9f..1912d691, review clean after 1 fix round)
Task 2: review found only "full-suite evidence missing" (implementer's 2-min timeout). Controller ran the suite: tests 5085 / pass 5082 / fail 0 / skipped 3, exit 0 (Node 24.21, root in container). Ruling: the accepted baseline signature in THIS container under Node 24 is `fail 0, skipped 3` (the two host-dependent tests self-skip as root on Linux); "suite green" for the rest of M1a means fail 0 and skipped ≤ 3 — cost if wrong: a genuinely failing host-dependent test would be masked; mitigated by CI on macOS runners later.
Task 2: minor (deferred): test name "covers at least five scenarios" / `>= 5` is stale (corpus has seven); the real enforcement is the SCENARIOS loop.
Task 2: complete (commits 1912d691..dac74652, review clean after controller-supplied suite evidence)
Task 3: implemented dac74652..e7011635. Ruling: any new scripts/*.mjs needs (1) a `!scripts/<name>.mjs` .gitignore allowlist entry and (2) an entry in DEPLOY_FILES in scripts/lib/deploy-integrity.mjs (the suite asserts deploy integrity) — carried into dispatches for Tasks 4, 6, 9, 10 — cost if wrong: a failing deploy-integrity test, caught by the suite.
Task 3: review Approved (all Minor). Controller folded three minors into follow-up a7f058b3 (invariant Exact<>, amendment policy, tsc resolved via createRequire; out-of-tree compiler warned on stderr). Ruling: keep the warn-and-run behaviour for a globally resolved typescript — cost if wrong: an unpinned compiler checks the contract in an odd layout; visible on stderr.
Task 3: minor (deferred): PLUR1BUS_TYPECHECK_OPTIONAL=1 is a green-without-checking switch (brief-mandated, warns); conformance.ts ships in the tarball (inert).
Task 3: complete (commits dac74652..a7f058b3, review clean)
Task 4: complete (commits a7f058b3..c67cc820, review clean). Minor folded into Task 5 dispatch: wrap the win32 `icacls` execFile in securePath so a missing binary/spawn failure returns `{ applied:false, reason }` instead of throwing (brief-inherited).
Task 5: fix round 1/5 (2 addressed, 0 open — ContractVersion 1.0.0→1.1.0 for the widened SecurePathResult.reason; icacls catch narrowed to ENOENT; commits 9efbfd97..8ed331ea). Ruling: widening the frozen contract is allowed with a minor bump per the file's own policy — cost if wrong: none (no consumer exists yet).
Task 5: fix round 2/5 (1 addressed — controller's full-suite run caught deploy-integrity: lib/platform.js missing from DEPLOY_FILES; commit 96fe0788). Ruling: the cheap tier skips the 7.5-min suite; from now on the controller runs the full suite for every cheap-tier task and records it in the report — cost: ~8 min per task.
Task 5: complete (commits c67cc820..96fe0788; suite 5114/5111/0 fail/3 skipped)
Task 6: fix round 1/5 (2 addressed — HostServices.workspaceDir made async, ContractVersion 1.1.0→1.2.0; stub override loop allowlist; commits dfab23ec..829b97e7). Ruling: the frozen contract carried a real defect (sync workspaceDir vs async resolvers everywhere) — corrected with a minor bump before any consumer exists — cost if wrong: none; a later host with a sync resolver still works (await of a non-promise).
Task 6: complete (commits 96fe0788..829b97e7, review clean)
Task 7: implemented 829b97e7..bc38fb21. Ruling: 14 `api.logger` sites inside four module-top-level functions declared before register() (inspectCronNativeCapabilities, reconcileUnsafeDirectCronsWithService, runDeferredFeatureCronBootstrap, makeReactionsCapabilityChecker — own `api` parameter, three are protected exports tested with hand-built api stubs) stay on `api.logger`; the guard test exempts them by name — cost if wrong: those four functions remain host-coupled until PR-14 moves them into the adapter; carried into Task 8 (same functions hold runtimeIfUsable(api) reads) and Task 9 (index.js is on the lint allowlist anyway).
Task 7: complete (commits 829b97e7..bc38fb21, review clean; suite 5131/5128/0/3)
Task 8: implemented bc38fb21..859f6b90. Ruling: a fifth pre-register() top-level function `resolveNeoHooksConfig(api, …)` (index.js:3310) joins the exempt set (kept on runtimeIfUsable(api)); exempt lists live in tests/index-host-logger.test.js — cost if wrong: one more host-coupled function until PR-14. Pass-through sites use `host.runtime ?? undefined` (3); probes use `host.runtime` (null vs undefined only changes the TypeError text on non-optional `.agent` reads).
Task 8: complete (commits bc38fb21..859f6b90, review clean; suite 5132/5129/0/3). Note for later refactors: credentialResolver.getConfig at ~index.js:4441 forward-references `host` (declared on the next line) — safe today because it is only called lazily.
Task 9: complete (commits 859f6b90..fd5bac5b, review clean; suite 5135/5132/0/3). Minor (deferred, brief-inherited): comment stripper is line-oriented; `host.api.` is not caught by the `\bapi\.` lint — folded into Task 10: lint-engine-imports also forbids `host.api` / `.api.` reads under engine/**.
Task 10: fix round 1/5 (5 addressed — analyser argument validation, registerStart self-check, lint fixtures in tmpdir, .mjs walk, require() pattern; commits c10d6b24..7e21bbd5)
Task 10: minor (deferred, latent): analyser drops undeclared globals silently; JSDoc type refs not counted (tasks moving JSDoc'd functions add type imports by hand); rule-4 `.api` check is line-based (string literal `"openai.api"` would false-positive; none exist in lib/ or index.js today).
Task 10: complete (commits fd5bac5b..7e21bbd5, review clean; suite 5164/5161/0/3 at c10d6b24)
Task 11: implemented 7e21bbd5..53f1ab18 (+ abea9503). Ruling: Task 10's fix round left the suite red (tests/lint-engine-imports.test.js used mkdtempSync directly and tripped the repo's "use the shared temp-dir helper" guard test) because that round skipped the full suite; the Task 11 implementer fixed it forward in abea9503 — accepted. New rule: every fix round that touches tests/ or scripts/ runs the full suite before re-review — cost: ~8 min per round.
Task 11: complete (commits 7e21bbd5..abea9503, review clean; suite 5171/5168/0/3). Pattern for 12–18: re-derive ranges by grep; manual dedent-diff for byte-identity; do not skip the RED step.
Task 12: complete (commits abea9503..bd16fb53, review clean; suite 5175/5172/0/3). Ruling: host.stateDir snapshots OPENCLAW_HOME at register time (old code re-read env per call) — accepted as behaviour-neutral (nothing mutates the env at runtime; golden driver sets it before register) — cost if wrong: a host that changes OPENCLAW_HOME mid-process, which none does. Minor: registerStart constant drifts every move → folded into Task 13: make the analyser derive registerStart by regex instead of a hand-maintained constant. Note for 13–18: tests/workspace-policy-runtime-gates.test.js counts `automaticWorkspacePolicyDecision(` call sites across index.js + engine files (sum must stay 4).
Task 13: implemented bd16fb53..153229f1 (2f38cdea analyser registerStart derived). Finding for 14–18: lazy `await import("./lib/…")` specifiers inside moved blocks are invisible to the analyser and the golden corpus — rewrite to `../../lib/…` and verify each resolves (deploy-integrity's graph walk is the net). Three tests extended to cross-file sums (workspace-policy-runtime-gates, llm-result-cache-integration, the brief's own boundary test probe moved to pool.withDb).
Task 13: complete (commits bd16fb53..153229f1, review clean; suite 5179/5176/0/3). Minor (deferred): lib/memory-request-context.js reaches openclaw/plugin-sdk/routing transitively via createHostRoutingLoader's default importRouting (injected from index.js today) — note for the milestone that claims full host-neutrality (PR-06/PR-14).
Task 14: implemented 153229f1..017c3335 (d59f6e28 state object first). Ruling: range extended by three lines (10357-10359) to include two turn-time `let` latches (warnedMissingCaptureSessionKey, warnedIncognitoClassifierDegraded) used only inside the block — declared in createTurnCapture's scope — cost if wrong: none (block-local). Deferred to end of PR-03 (Task 18): prune dead index.js imports (~100 by then) in one commit; introduce a shared readRuntimeSources() helper for the literal-source guard tests (five now redirected/summed).
Task 14: complete (commits 153229f1..017c3335, review clean; suite 5183/5180/0/3). Minor (deferred to Task 18 cleanup): dead index.js imports (~82 now); tests/llm-result-cache-integration.test.js:519 negative guard checks index.js only (asymmetry); report §5.4 prose misstates registration order (code is correct: capture → reply-outcome → turn-route).
Task 15: implemented 017c3335..32f71f5e (37b9822f prep hoists confirmationStore/confirmationIndex Maps above the factory call). Ruling: thunk list is 10 (6 command bodies + checkAuth, checkArgsLength, resolveDenialLocale, resolveRegisteredMemoryContext); two Maps shared by hoisting — cost if wrong: none (same objects). Ruling: bare `api` value passed to resolveNeoHooksConfig inside the block becomes a named ctx capability `resolveNeoHooksConfig(commandConfig)` (partial application in index.js) — cost if wrong: none; noted for PR-04: add a bare-identifier `\bapi\b` check for engine/**. Carry to Task 16: the 10 thunk names; cfg `let` reassigned at :4561 — Task 16's block is closer to it.
Task 15: complete (commits 017c3335..32f71f5e, review clean; suite 5187/5184/0/3). Ruling: add a bare-identifier `\bapi\b` gate for engine/** in Task 18 (cheap, closes the f(api, …) blind spot) rather than PR-04. Minor deferred: b13 vacuous handleObsidianBridgeCommand assertion (pre-existing); ~40 more dead index.js imports.
Task 16: implemented 32f71f5e..b365cead. Ruling: registerChatCommands returns all ten thunk targets (six commands + checkAuth, checkArgsLength, resolveDenialLocale, resolveRegisteredMemoryContext), rebound in index.js via `let` at :7273-7279 — cost if wrong: none (same closures, names preserved). `/wiki` registration stays in index.js (analyser count settled the boundary) — Task 18 README must list it. Carry to 17/18: shared readRuntimeSources() test helper; dead-import cleanup; bare `\bapi\b` engine gate.
Task 16: complete (commits 32f71f5e..b365cead, review clean; suite 5192/5189/0/3). Minor deferred to Task 18 cleanup: drop the adapter from allRuntimeSources in workspace-policy-runtime-gates (contributes 0 matches); b13 comment numbers wrong (12→6, 8→2); stale `index.js:9226` message in the new adapter test; dead imports now ~217. Observation: PR-04's split is ten bodies + five helpers.
Task 17: implemented b365cead..99d480a7. Ruling: the brief's register-tools snippet dropped `{ names: [...] }` — restored verbatim (behaviour-neutrality) — cost if wrong: none. Note: analyser fingerprint alone can confirm a bad range; pair with a brace-balance/node --check test. Engine-lint is text-based: JSDoc prose in engine/** must not spell `api.`.
Task 17: complete (commits b365cead..99d480a7, review clean; suite 5196/5193/0/3). Minor deferred: dead imports ~230/475; knowledgeToolSection anchor shortened (loud on drift).
Task 18: implemented 99d480a7..e0fcc45a (A deb3b3c1, B1 d39d49ed prune, B2 a1bbcd2c helper, B3 e0fcc45a bare-api rule). Ruling: gateway/cron ranges are NOT consolidated into two functions — each range keeps its own exported function at its original call position because registerChatCommands registers gateway_start/stop between the bridge and Neo pairs and a test reads the last before_agent_reply handler; consolidating would reorder host handler lists (GC2) — cost if wrong: PR-04 has to hoist service definitions itself; recorded in README/commit.
Task 18: complete (commits 99d480a7..e0fcc45a, review clean; suite 5214/5211/0/3). Minor (deferred to final review): lint-engine-imports docblock overstates comment stripping (multi-line block comments without `*` prefix false-positive); runtime-sources.js JSDoc summary says repo-relative, returns absolute; README says "three pairs" (two pairs + one start); workspace-policy-runtime-gates lost the comment for memoryToolsSource; commit d39d49ed scope `index` not in GC10 list.
Task 19: implemented e0fcc45a..3af5e1fe (c2295d07 probe; bfbe34e3 per-phase sink; 3af5e1fe setup/recall split + conditional fold). Ruling: per-phase timings are exposed additively via an optional `recallTimingSink` ctx key, sourced in index.js from a test-only stub-api property (`api.__recallTimingSinkForTests ?? null`); namespace-phase folding runs only when a sink is attached, so production paths are unchanged — cost if wrong: none (dead path in production). B6 data (stub embedder, synthetic corpus, recall-only): ×1 worst p95 242 ms; ×5 recall-truncated p50 612 / p95 808 ms.
Task 19: fix round 3/5 (all 4 addressed — runtime locate() citations, results file committed, guarded sink try/catch, phase table partition; commits 3af5e1fe..c95c94c4). Reviewer's ×5 p95 delta (807.5→663.4 ms) is benchmark noise (N=20, p95=p99=max, floor); not a code change.
Task 19: complete (commits e0fcc45a..c95c94c4, review clean; suite 5225/5222/0/3). Docs nit (deferred to Task 20): results file should state that p95 equals max at N=20.
Task 20: implemented c95c94c4..f6ded33d. Ruling: `.gitignore` gains `!docs/engine-api.md` (docs/** is denylisted; without it the shipped doc could never be tracked) — one-line, non-code — cost if wrong: none. Brief's "eight chmod sites" and the addendum's "14 logger sites" left unnumbered in the CHANGELOG (tree counts 9 securePath call sites incl. one seam default; 14 api.logger lines). Reviewer NIT fixed by controller in 0c5e3f69 (types/engine.d.ts header said 1.1.0; comment-only, typecheck green).
Task 20: complete (commits c95c94c4..0c5e3f69, review clean; suite 5225/5222/0/3 at f6ded33d).
All 20 tasks complete. Whole-branch review next: package review-89148f9f..HEAD.diff (21 620 lines, 87 files).
Whole-branch review (Opus, fresh): MERGEABLE AFTER FIX WAVE, no BLOCKER, all rulings AGREE. Full report + fix list + PR-description items + owner-gate notes: whole-branch-review.md (same dir).
PAUSED 2026-09-22 17:25 at owner request (quota). Resume 2026-09-23 15:00: (1) fix wave per whole-branch-review.md §"Fix wave" (12 items, docs/tests/scripts only, no code behaviour) — Sonnet implementer, full suite after; (2) scoped re-review of fix diff; (3) bundle → Mac ~/openclaw-plur1bus-memory (at stale b4138df5: fetch main first) → push feat/engine-extraction-m1a → PR with the PR-description items; (4) owner gate with the 7 owner-gate notes + rulings list; (5) M1b plan. Worktree clean except untracked plan file; HEAD 0c5e3f69; suite 5225/5222/0/3 at f6ded33d.
Fix wave: 0c5e3f69..cba055de (f9b88635 tests/scripts, cba055de docs); suite 5225/5222/0/3; re-review pending
Fix wave: re-review ALL ADDRESSED 12/12, no new breakage (0c5e3f69..cba055de). Branch final at cba055de; suite 5225/5222/0/3. Next: push + PR.
Pushed feat/engine-extraction-m1a (cba055de) to origin via Mac; PR #184 open: https://github.com/Cyb3rb1ade/openclaw-plur1bus-memory/pull/184. M1a execution complete; owner gate pending. SDD workspace kept until owner gate (rulings list delivered in chat).
